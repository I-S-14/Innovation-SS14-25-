# Диздок: МОД-костюмы Innovation Station

Полная переработка МОД-сьютов «с нуля» по мотивам `/tg/station`, но с главным
дополнительным требованием: **бо́льшая часть системы должна быть переиспользована
будущим реворком мехов**. Поэтому проект делится на два слоя — универсальный
каркас «модульного шасси» и надстройка «костюм» поверх него.

Источники: `BandaStation-master/code/modules/mod/*` (форк /tg/station),
текущая Goob-реализация в `Content.Goobstation.Shared/Clothing/*`.

---

## 1. Философия и принципы

**МОД-костюм — это не «броня с кнопкой», а платформа, которую игрок собирает под себя.**

1. **Костюм = ядро + обшивка + модули.** Ни одна из трёх частей сама по себе не играет.
   Ядро даёт энергию, обшивка — внешний вид, броню и лимит сложности, модули — весь функционал.
2. **Функционал живёт в модулях, а не в прототипе костюма.** Два инженера с одинаковой
   обшивкой должны собираться по-разному. Если фича «вшита» в костюм — это ошибка дизайна.
3. **Бюджет сложности — главный источник решений.** Всё влезть не может никогда.
   Игрок выбирает между «фонарь + хранилище» и «джетпак».
4. **Три состояния, а не два.** Сложен → развёрнут → загерметизирован. Промежуточное
   состояние «развёрнут, но не запечатан» — это ключ к тому, чтобы костюм не был
   «просто скафандром»: в нём можно ходить без штрафов, но он не держит давление.
5. **Костюм — вещь, которую можно вскрыть.** Панель проводов, ID-замок, отсек с батареей,
   отвёртка и лом — всё это контент для взломщика ничуть не меньше, чем для владельца.
6. **Новый модуль — это YAML, а не C#.** Библиотека готовых поведений закрывает 80% идей.
   C# пишется только под действительно уникальную механику.

**Анти-цели:** костюм-«всё-в-одном» без выбора; модули, каждый из которых требует своей системы;
жёсткая привязка к слоту «спина» на уровне кода; невозможность переиспользовать код в мехах.

---

## 2. Что не так с текущей реализацией

Сейчас в билде две несвязанные ветки МОД-сьютов (Goob и EinsteinEngines) и обе —
не модульные системы, а набор захардкоженных костюмов:

| Проблема | Как сейчас |
|---|---|
| Нет модулей вообще | Весь функционал прибит к прототипу конкретного костюма (`ComponentToggler` с `PressureProtection` прямо в нагруднике) |
| Нет ядра и нет энергии как ресурса | `SealableClothingRequiresPower` — единственная связь с питанием, «есть заряд / нет заряда» |
| Нет UI | Есть только экшен «загерметизироваться» |
| Нет панели проводов и ID-замка | Отсутствуют |
| Нет бюджета сложности | Отсутствует |
| Нет сборки | `Construction`-граф собирает целиком готовый костюм фиксированной модели |
| Развёртывание частей — хак поверх `ToggleableClothing` | Апстримный компонент раздут Goob'ом (`ClothingPrototypes`, `EquippedPrefixes`, `ClothingUids`) — правки в апстримном файле, конфликты при синках |
| Тема/обшивка не существует как понятие | Каждый скин — отдельное дерево из 5 прототипов с копипастой брони |

Итог: чтобы добавить костюм, нужно скопировать ~250 строк YAML; чтобы добавить фичу — писать систему.
Это ровно то, что новая архитектура должна убрать.

---

## 3. Архитектура: два слоя

```
+-----------------------------------------------------------------+
|  Content.Shared/_IS14/Modular/       МОДУЛЬНОЕ ШАССИ            |
|  (переиспользуется мехами)                                      |
|                                                                 |
|   ModularChassisComponent   — хост модулей, бюджет сложности    |
|   ChassisModuleComponent    — предмет-модуль, тип, стоимость    |
|   ChassisPowerComponent     — абстракция «откуда заряд»         |
|   ModuleBehaviour*Component — библиотека готовых поведений      |
|   события установки/включения/использования                     |
|   BUI + окно со вкладками + радиальное меню                     |
+-----------------------------------------------------------------+
                 ^                              ^
                 |                              |
+----------------+--------------+  +------------+-------------------+
| Content.Shared/_IS14/Modsuit/ |  | Content.Shared/_IS14/Mech/     |
|  ModsuitControlComponent      |  |  (будущий реворк мехов)        |
|  ModsuitPartComponent         |  |  кабина, оборудование, ножницы |
|  ModCoreComponent             |  |  — те же модули, тот же UI     |
|  развёртывание / герметизация |  |                                |
|  скины, обшивка               |  |                                |
+-------------------------------+  +--------------------------------+
```

Правило разделения: **всё, что не знает слова «одежда», «слот инвентаря» и «носитель» —
живёт в `Modular/`.** Всё, что знает — в `Modsuit/`.

Проверка на переиспользуемость: если мех не сможет использовать компонент как есть —
компонент лежит не в том слое.

---

## 4. Структура сущностей

МОД-костюм — это **одна сущность-контроллер** (носится в слоте `back`), внутри которой лежит всё остальное.

```
ModsuitControl (сущность, слот back)
├── контейнер  "mod-parts"          — 4 части костюма, когда сложены
│     ├── ModsuitHelmet     (слот head)
│     ├── ModsuitChestplate (слот outerClothing)
│     ├── ModsuitGauntlets  (слот gloves)
│     └── ModsuitBoots      (слот shoes)
├── контейнер  "mod-modules"        — установленные модули
│     ├── ModuleFlashlight
│     ├── ModuleStorage
│     └── ...
├── ItemSlot   "mod-core"           — ядро (обязательно для работы)
│     └── ModCoreStandard
│           └── ItemSlot "cell_slot" — батарея (PowerCellSlot)
├── WiresPanel + Wires              — панель проводов
├── AccessReader                    — ID-замок
└── ActivatableUI                   — интерфейс костюма
```

Части — **настоящие сущности одежды**. При развёртывании они переезжают из контейнера
в слоты инвентаря носителя и получают `Unremoveable`, при сворачивании — обратно.
Это позволяет частям иметь собственную броню, собственные спрайты `equipped-*`,
собственный `Storage` (нагрудник) и собственные слои модульных оверлеев.

### Почему набор частей не фиксирован

`ModsuitControlComponent.Parts` — это словарь `SlotFlags → EntityUid`, а не четыре поля.
Это даёт бесплатно: костюмы из 2 частей (лёгкий «портативный» скафандр), костюмы с маской
вместо шлема, костюмы без перчаток. Модули объявляют требуемые слоты через тот же `SlotFlags`.

---

## 5. Три состояния костюма

```
   СЛОЖЕН                РАЗВЁРНУТ                ЗАПЕЧАТАН (активен)
 +----------+   deploy  +----------+  activate   +----------+
 | рюкзак   | --------> | надет    | ----------> | активен  |
 | на спине | <-------- | целиком  | <---------- |          |
 +----------+  retract  +----------+ deactivate  +----------+
```

| | Сложен | Развёрнут | Запечатан |
|---|---|---|---|
| Части в слотах | нет | да | да |
| Броня частей | нет | **да** | да |
| Держит давление / вакуум | нет | нет | **да** |
| Тепло/холодозащита | нет | нет | **да** |
| Скрывает волосы/лицо/маску | нет | нет | **да** |
| Замедление | нет | нет | **да** (по теме) |
| Расход энергии | нет | нет | **да** |
| Модули работают | нет | нет* | **да** |
| Можно снять костюм | да | нет (части надеты) | нет |

\* кроме модулей с флагом `AllowInactive`.

**Развёртывание** — мгновенное, по частям (радиальное меню) или всё разом.
**Герметизация** — последовательная, ~1 с на часть, с попапом и звуком на каждую
(«ботинки защёлкиваются», «нагрудник герметизируется», …). Прерывается движением/уроном.
Это ощутимая пауза перед выходом в космос — важный элемент напряжения, его нужно сохранить.

**Частичная герметизация возможна:** можно свернуть шлем на запечатанном костюме — он
расшивается отдельно, остальные части остаются запечатанными. Модули, требующие шлема,
при этом отключаются. Это то, что делает систему живой.

---

## 6. Ядро и энергия

### Модель

`ChassisPowerComponent` на шасси не знает, откуда берётся заряд. Он поднимает события:

```csharp
ChassisGetChargeEvent(out float Current, out float Max)
ChassisTryUseChargeEvent(float Amount, out bool Handled)
ChassisAddChargeEvent(float Amount)
```

Отвечает на них тот, кто может — обычно `ModCoreComponent` на ядре в слоте.
Мех подпишет на те же события свою батарею и получит весь UI зарядки бесплатно.

### Типы ядер

| Ядро | Источник заряда | Особенность |
|---|---|---|
| **Стандартное** | обычный power cell в своём `ItemSlot` | батарею можно менять и заряжать в зарядниках |
| **Плазменное** | сжигает листы плазмы | самозарядка, но нужен расходник; лаваленд-версия |
| **Реакторное** | встроенная батарея большой ёмкости | не меняется, заряжается только в зарядной станции |
| **Бесконечное** | — | админское / антаг-топ |

Ядро обязательно: без него костюм разворачивается, но не герметизируется и не даёт модулей.

### Числа (стартовая калибровка, тюнится)

Ёмкости батарей в билде: Small 360 Дж, Medium 720, High 1080, Hyper 1800.

| Параметр | Значение |
|---|---|
| Базовый расход запечатанного костюма | **0.5 Вт** |
| Стандартное ядро + Medium cell | 720 Дж → **24 мин** в вакууме без модулей |
| Стандартное ядро + Hyper cell | 1800 Дж → **60 мин** |
| Пассивный модуль | 0 Вт |
| Включаемый модуль (лёгкий: фонарь, визор) | 0.1–0.3 Вт |
| Включаемый модуль (тяжёлый: джетпак, магнитные ботинки) | 0.8–2 Вт |
| Разовое использование (дефибриллятор, инъектор) | 10–40 Дж за раз |
| Множитель расхода по теме | ×0.25 (гражданский) … ×2 (прототип) |

При нулевом заряде костюм принудительно расшивается (не мгновенно — с попапом-предупреждением
за ~10% заряда).

---

## 7. Модули

### 7.1 Четыре типа

| Тип | Поведение | Примеры |
|---|---|---|
| **Passive** | работает всегда, пока модуль установлен и части на месте | хранилище, защита от ЭМИ, антискольз, долгое падение |
| **Toggleable** | вкл/выкл, ест энергию пока включён | фонарь, визор, терморегулятор, магнитные ботинки, джетпак |
| **Usable** | разовое срабатывание + кулдаун | инъектор, диспенсер, «микроволновый луч» |
| **Active** | одновременно активен только один; даёт предмет в руки **или** особый клик по цели | дефибриллятор, дрель, гидрозахват, кинезис, медлуч |

### 7.2 Поля `ChassisModuleComponent`

```csharp
ModuleKind   Kind;              // Passive | Toggleable | Usable | Active
int          Complexity;        // цена в бюджете шасси
float        IdleDraw;          // Вт, пока установлен
float        ActiveDraw;        // Вт, пока включён
float        UseCost;           // Дж за срабатывание
TimeSpan     Cooldown;
bool         Removable = true;  // встроенные модули темы — false, Complexity = 0
List<SlotFlags> RequiredSlots;  // [HEAD, GLOVES|FEET] — «И» по элементам, «ИЛИ» внутри
ProtoId<TagPrototype>[] Conflicts;  // теги несовместимости (двусторонняя проверка)
ModuleAllowFlags Allow;         // Unworn | ChassisInactive | Incapacitated
```

`RequiredSlots` — точная копия удачной идеи /tg/: модуль дефибриллятора требует перчаток,
визор — шлема, джетпак — нагрудника. Если часть свёрнута или расшита — модуль автоматически
отключается и подсвечивается в UI как «недоступен».

Конфликты через **теги**, а не через типы: `Conflicts: [ModsuitVisor]` — все визоры
несовместимы друг с другом, но модуль ночного зрения (не-визор) с ними уживается.

### 7.3 Событийная модель

Модуль сам по себе ничего не делает — он рассылает события, на которые отвечают
компоненты-поведения, висящие на той же сущности модуля.

| Событие | Когда |
|---|---|
| `ModuleInstalledEvent` / `ModuleUninstalledEvent` | установка в шасси / изъятие |
| `ModuleEnabledEvent` / `ModuleDisabledEvent` | требования по частям выполнены / нарушены |
| `ModuleActivateAttemptEvent` | попытка включить (отменяемое) |
| `ModuleActivatedEvent` / `ModuleDeactivatedEvent` | включён / выключен |
| `ModuleUsedEvent(user, target?)` | разовое срабатывание |
| `ModuleGetUiDataEvent` | сбор кастомных данных для UI |
| `ModuleGetConfigEvent` / `ModuleConfigChangedEvent` | настройки модуля из UI |
| На шасси: `ChassisModulesChangedEvent`, `ChassisPowerChangedEvent`, `ChassisStateChangedEvent` | |

### 7.4 Библиотека поведений (главная инвестиция в гибкость)

Эти компоненты пишутся **один раз** и покрывают подавляющее большинство модулей.
После них новый модуль = один YAML-прототип.

| Компонент | Что делает | Покрывает модулей |
|---|---|---|
| `ModuleGrantComponentsComponent` | навешивает компоненты на цель (`Wearer` / `Chassis` / `Part`) пока модуль включён | ~40% всего: все визоры и HUD'ы, ночное зрение, защита от радиации, антискольз, терморегулятор, сварочная защита, стабилизатор плазмы, долгое падение |
| `ModuleActionComponent` | выдаёт носителю экшен | джетпак, магнитные ботинки, тросомёт |
| `ModuleItemComponent` | держит сущность-девайс, выдаёт её в руки при активации (`Active`) | дефибриллятор, анализатор здоровья, дрель, гидрозахват, магнит, сканер реагентов |
| `ModuleStorageComponent` | включает `Storage` заданного объёма на нагруднике | все хранилища, рудный мешок, кобура |
| `ModuleSpeedModifierComponent` | правит скорость носителя | «торсионная трещотка», компрессия пластин |
| `ModuleArmorModifierComponent` | добавляет коэффициенты брони частям | усилитель брони, компрессия пластин |
| `ModuleLightComponent` | `PointLight` на костюме + спрайтовый оверлей | фонарь, «фонарь тьмы» |
| `ModuleTargetedActionComponent` | клик по цели с кулдауном и радиусом (`Active` без предмета) | кинезис, медлуч, захват преступника |
| `ModuleWornOverlayComponent` | спрайтовый оверлей поверх части (состояния вкл/выкл/кулдаун) | визуал ~30 модулей |
| `ModuleAlertComponent` | алерт на HUD носителя | статус-ридаут, ДНК-замок |

Оставшиеся 20% (кинезис, физика джетпака, стелс) требуют своей системы — и это нормально.

### 7.5 Каталог MVP

Стартовый набор, который делает систему играбельной. Cx — сложность.

**Общие**

| Модуль | Тип | Слот | Cx | Комментарий |
|---|---|---|---|---|
| Компактное хранилище | Passive | нагрудник | 3 | 30 ед. объёма |
| Хранилище | Passive | нагрудник | 5 | 60 ед. |
| Блюспейс-хранилище | Passive | нагрудник | 8 | 120 ед., РнД |
| Фонарь | Toggleable | шлем | 1 | |
| Статус-ридаут | Passive | — | 0 | здоровье/давление/температура в UI и алертах |
| Отверстие для рта | Passive | шлем | 1 | есть/пить не расшиваясь, ценой герметичности головы |
| Защита от ЭМИ | Passive | — | 3 | |
| Терморегулятор | Toggleable | нагрудник | 2 | держит комфортную температуру тела |
| Стабилизатор плазмы | Passive | — | 2 | для плазмачеловека |
| Долгое падение | Passive | ботинки | 2 | |
| Держатель шляпы | Passive | шлем | 1 | шляпа поверх шлема |
| ДНК-замок | Passive | — | 2 | костюм работает только на владельце |
| Джетпак | Toggleable | нагрудник | 5 | |
| ГПС | Passive | — | 1 | |

**Инженерия**

| Модуль | Тип | Слот | Cx | Комментарий |
|---|---|---|---|---|
| Сварочная защита | Toggleable | шлем | 2 | защита от вспышек + мезоны |
| Магнитные ботинки | Toggleable | ботинки | 3 | |
| Защита от радиации | Passive | — | 4 | |
| Т-луч сканер | Toggleable | шлем | 2 | |
| Аварийный трос | Usable | перчатки | 3 | притягивает к поверхности |
| Распылитель | Active | перчатки | 3 | вода / пена / резина |

**Медицина**

| Модуль | Тип | Слот | Cx | Комментарий |
|---|---|---|---|---|
| Анализатор здоровья | Active | перчатки | 2 | |
| Дефибриллятор | Active | перчатки | 5 | |
| Инъектор | Usable | перчатки | 4 | настраиваемый реагент |
| Быстрый перенос | Passive | перчатки | 2 | ускоряет подъём тел |
| Мед-визор | Toggleable | шлем | 2 | |

**Безопасность**

| Модуль | Тип | Слот | Cx | Комментарий |
|---|---|---|---|---|
| СБ-визор | Toggleable | шлем | 2 | |
| Кобура | Passive | нагрудник | 3 | |
| Мегафон | Usable | шлем | 1 | |
| Магнитная подвеска | Passive | нагрудник | 3 | оружие не выбить из рук |
| Перечные плечи | Usable | нагрудник | 4 | |

**Снабжение / шахта**

| Модуль | Тип | Слот | Cx | Комментарий |
|---|---|---|---|---|
| Рудный мешок | Passive | нагрудник | 3 | |
| Дрель | Active | перчатки | 4 | |
| Гидравлический захват | Active | перчатки | 4 | |
| Пепловое наращивание | Passive | — | 3 | броня растёт на лаваленде |

**Наука / антаг** (после MVP) — кинезис, телепорт, антигравитация, стелс, энергощит, медлуч.

---

## 8. Тема, обшивка, скин

Три разных понятия, которые важно не смешать:

- **Тема (theme)** — «инженерный», «военный», «гражданский». Задаёт статы: броня, лимит сложности,
  расход, замедление, набор встроенных модулей, разрешённое содержимое кармана нагрудника.
- **Обшивка (plating)** — предмет, который вставляется при сборке и определяет тему.
- **Скин (skin)** — вариант внешнего вида внутри темы. Меняется набором для перекраски.

В SS14 тема реализуется как **прототип-родитель**:
`ModsuitControlBase` → `ModsuitControlEngineering` → конкретные варианты.
Отдельного `ModThemePrototype` не заводим — иерархия прототипов уже даёт наследование статов,
а `ModsuitSkinComponent` со списком доступных скинов даёт перекраску.

```yaml
- type: ModsuitSkin
  current: engineering
  skins:
    engineering: { rsi: _IS14/Clothing/Modsuits/engineering.rsi }
    atmospheric: { rsi: _IS14/Clothing/Modsuits/atmospheric.rsi }
```

Смена скина меняет `rsi` у всех частей разом.

### Темы MVP (под уже имеющиеся спрайты)

| Тема | Cx | Расход | Замедл. | Броня | Встроенные модули |
|---|---|---|---|---|---|
| Стандартный | 15 | ×1 | 0.75 | лёгкая | — |
| Гражданский | 12 | ×0.5 | 0 | нет | — |
| Инженерный | 15 | ×1 | 1.0 | средняя, +рад | сварочная защита |
| Атмосферный | 15 | ×1 | 1.0 | средняя, +жар | терморегулятор |
| Медицинский | 15 | ×1.5 | 0.5 | лёгкая | мед-визор |
| Исследовательский | 20 | ×1 | 1.25 | лёгкая | — |
| Военный (СБ) | 13 | ×1 | 0.5 | тяжёлая | СБ-визор |
| Шахтёрский | 13 | ×2 | 0.75 | средняя, +тепло | пепловое наращивание |
| Погрузочный | 10 | ×1 | 0 | средняя | гидрозахват |
| Магнат (капитан) | 20 | ×1 | 0.25 | тяжёлая | — |
| Синдикат | 20 | ×1 | 0 | тяжёлая | — |

---

## 9. Сборка и производство

Цепочка, повторяющая /tg/, но на `Construction`-графе SS14:

```
пустой корпус (ModsuitShell)
   + ядро                                        -> корпус с ядром
   + отвёртка                                    -> закреплено
   + шлем / нагрудник / перчатки / ботинки       -> полный набор
   + гаечный ключ                                -> скручено
   + отвёртка                                    -> готово к обшивке
   + обшивка (plating темы)                      -> ГОТОВЫЙ КОСТЮМ
```

- Заготовки частей и корпус печатаются в **экзосьют-фабрикаторе** (сталь).
- Обшивки — по темам, каждая за своим ресёрчем и своим набором материалов
  (инженерная — золото, атмос — титан, мед — серебро, СБ — плазма).
- Модули печатаются отдельно, каждый за своим узлом техвеба.
- Ядра: стандартное — базовый ресёрч; плазменное — через плазму/лаваленд; реакторное — поздний ресёрч.

Это делает МОД-костюм **проектом отдела РнД на раунд**, а не предметом из шкафчика.
Стартовые (в шкафчиках) — только гражданский и стандартный, без модулей.

---

## 10. Панель проводов, замок, взлом

Панель открывается **отвёрткой** только когда костюм не активен и не развёрнут.
С открытой панелью доступны: установка модуля (клик модулем), извлечение (лом),
смена ядра (гаечный ключ), провода (кусачки/мультитул), перепись доступа (ID-карта).

| Провод | Импульс | Перерезать | Починить |
|---|---|---|---|
| **Hack** | переключить ID-замок | стереть весь доступ | — |
| **Disable** | сбой на время | костюм неисправен: ×3 расход, модули случайно вырубаются | вернуть норму |
| **Shock** | шибает током на время | шибает постоянно | снять |
| **Interface** | сломать/починить UI | UI недоступен | вернуть |
| ×2 пустышки | — | — | — |

`WiresSystem` в SS14 серверная, поэтому действия проводов — `Content.Server/_IS14/Modsuit/Wires/`.

**ID-замок:** пока заперт — нельзя открыть панель, вытащить модули и (опционально) активировать.
Снимается ID-картой с нужным доступом, ЭМАГом или проводом Hack.

**EMP:** отключает активный модуль, наносит небольшой ожог носителю, с шансом временно
ломает интерфейс.

---

## 11. Интерфейс

`ActivatableUI` + `BoundUserInterface`. Окно — **общее для костюма и будущего меха**
(`ModularChassisWindow`), поэтому оперирует только понятиями шасси/модулей/заряда,
а «части костюма» приходят отдельной секцией через данные состояния.

По правилам проекта: вся вёрстка в `.xaml`, логика в `.xaml.cs` с `[GenerateTypedNameReferences]`,
корневой тег = тип класса.

**Вкладка «Статус»**
- Полоса заряда (цвет + текст «540 / 720 Дж (75%)»), имя ядра
- Полоса сложности `9 / 15`
- Имя и должность носителя (с ID)
- Кнопка «Активировать / Деактивировать»
- Ряд частей: иконка + название + кнопка развернуть/свернуть у каждой
- Индикаторы: замок, панель, неисправность, поломка интерфейса

**Вкладка «Модули»**
Список карточек. Каждая карточка:
- иконка модуля, название, описание
- бейдж типа (пассивный / включаемый / разовый / активный)
- строка «Сложность 3 · 0.2 Вт · 25 Дж/исп.»
- кнопка действия (включить/выключить/использовать/выбрать), заблокирована с причиной
  («нужен шлем», «нет заряда», «кулдаун 4 с» — с прогресс-баром кулдауна)
- кнопка «закрепить» — вынести модуль на панель экшенов
- блок настроек, если модуль их отдаёт

**Настройки модулей — универсальные дескрипторы** (прямое заимствование удачной идеи /tg/):

```csharp
enum ModuleConfigKind { Bool, Number, Color, Choice, Button }
record ModuleConfigEntry(string Key, string Label, ModuleConfigKind Kind, object Value, object[]? Choices);
```

Так модуль инъектора отдаёт выбор реагента, фонарь — цвет и радиус, а UI не знает про них ничего.

**Вкладка «Железо»**
- Слот ядра (можно вынуть при открытой панели)
- Слот батареи внутри ядра — **проброшен наверх**, чтобы игрок видел «отсек с батареей» костюма
- Состояние панели, список доступа замка

**Вне окна:**
- Экшены на панели: «Развернуть», «Активировать», «Модули» (радиалка), «Панель» (открыть UI)
- Радиальное меню модулей (`SimpleRadialMenu`) — быстрый выбор без открытия окна;
  подсветка выбранного/включённого, затемнение на кулдауне
- Закреплённые модули — отдельные экшены в панели
- Алерт заряда костюма на HUD (как у скафандров)

---

## 12. Визуал

### Спрайты частей

RSI на скин, состояния: `control`, `control-sealed`, `helmet`, `helmet-sealed`, `chestplate`,
`chestplate-sealed`, `gauntlets`, `gauntlets-sealed`, `boots`, `boots-sealed` +
`equipped-BACKPACK/HEAD/OUTERCLOTHING/HAND/FEET` в обычной и `-sealed` версиях.
Это ровно тот формат, что уже лежит в `_Goobstation/Clothing/OuterClothing/Modsuits/*.rsi`
и `_EinsteinEngines/Clothing/NanoTrasen/Modsuits/*.rsi` — конвертация уже сделана, переиспользуем.

### Оверлеи модулей — техническое ограничение

`ClothingComponent.ClothingVisuals` помечен `[DataField]` **без** `[AutoNetworkedField]` —
изменения на сервере до клиента не доедут. Значит оверлеи модулей делаются **клиентской системой**:

`Content.Client/_IS14/Modsuit/ModsuitVisualsSystem` читает сетевое состояние
`ChassisModuleComponent` (включён / на кулдауне / активен) и вставляет дополнительные
слои в спрайт носителя при `EquipmentVisualsUpdatedEvent` соответствующей части.
`ModuleWornOverlayComponent` описывает: RSI, состояния для трёх режимов, целевой слот,
нужна ли маска по силуэту части, красить ли в цвет костюма.

Это единственное место, где потребуется аккуратная работа с `ClientClothingSystem` —
заложен отдельный спайк в фазе 6.

### Покраска

Набор для перекраски (`ModsuitPaintKit`) — меняет `color` всех частей.
Аппликатор скина — меняет `ModsuitSkinComponent.current`.

---

## 13. Что именно переиспользуют мехи

Явный чек-лист, по которому проверяется правильность разделения:

| Отдаём мехам как есть | Не отдаём |
|---|---|
| `ChassisModuleComponent` + все `ModuleBehaviour*` | развёртывание частей |
| Бюджет сложности и проверки совместимости | герметизация |
| `ChassisPowerComponent` и события заряда | скины/обшивка одежды |
| Библиотека поведений (grant/action/item/storage/light/targeted) | оверлеи на носителе |
| `ModularChassisWindow` + BUI + дескрипторы настроек | ID-замок в текущем виде (мех даст свой) |
| Радиальное меню модулей | |
| Установка/извлечение модулей инструментами | |

Практически: **оборудование меха = модуль типа `Active` с `ModuleItemComponent`.**
Дрель меха и дрель костюма — один прототип с разной сложностью.

---

## 14. План реализации

### Фаза 0 — Подготовка (спрайты + решения)
- [ ] Конвертировать DMI → RSI (см. раздел 15)
- [ ] Завести `Resources/Textures/_IS14/Clothing/Modsuits/` и `_IS14/Objects/Modsuit/`
- [ ] Пометить Goob/EE МОД-сьюты как legacy (не удалять — мигрируем в фазе 8)

### Фаза 1 — Каркас модульного шасси
`Content.Shared/_IS14/Modular/`
- [ ] `Components/ModularChassisComponent.cs`, `ChassisModuleComponent.cs`, `ChassisPowerComponent.cs`
- [ ] `ModularChassisEvents.cs` — весь набор событий из §7.3
- [ ] `Systems/SharedModularChassisSystem.cs` — установка/извлечение, бюджет сложности, конфликты по тегам
- [ ] `Systems/SharedChassisModuleSystem.cs` — включение/выключение/использование, кулдауны, требования по слотам
- [ ] `Systems/ChassisPowerSystem.cs` — расход по тику, обработка «сел заряд»
- [ ] Тесты: установка сверх лимита, конфликт тегов, снятие модуля во включённом состоянии

### Фаза 2 — Костюм: части, развёртывание, герметизация
`Content.Shared/_IS14/Modsuit/`
- [ ] `ModsuitControlComponent.cs`, `ModsuitPartComponent.cs`, `ModCoreComponent.cs`
- [ ] `SharedModsuitSystem.Parts.cs` — deploy/retract, `Unremoveable`, блок снятия костюма
- [ ] `SharedModsuitSystem.Seal.cs` — очередь герметизации, DoAfter на часть, попапы, звуки
- [ ] `SharedModsuitSystem.State.cs` — три состояния, включение/выключение питания
- [ ] `ModsuitPartVisualsSystem` — переключение `-sealed` слоёв
- [ ] Тесты: расшить одну часть на активном костюме, снять костюм с развёрнутыми частями

### Фаза 3 — Интерфейс
`Content.Client/_IS14/Modular/`
- [ ] `ModularChassisBoundUserInterface.cs`
- [ ] `ModularChassisWindow.xaml` + `.xaml.cs` (3 вкладки)
- [ ] `ModuleCard.xaml` + `.xaml.cs`
- [ ] `ModuleConfigControl.xaml` — рендер дескрипторов настроек
- [ ] `ModuleRadialMenu` на `SimpleRadialMenu`
- [ ] Экшены: развернуть / активировать / модули / панель
- [ ] Алерт заряда

### Фаза 4 — Библиотека поведений модулей
`Content.Shared/_IS14/Modular/Behaviours/`
- [ ] `ModuleGrantComponentsComponent` + система (приоритет №1, покрывает больше всего)
- [ ] `ModuleActionComponent`
- [ ] `ModuleItemComponent`
- [ ] `ModuleStorageComponent`
- [ ] `ModuleSpeedModifierComponent`, `ModuleArmorModifierComponent`
- [ ] `ModuleLightComponent`
- [ ] `ModuleTargetedActionComponent`
- [ ] `ModuleAlertComponent`

### Фаза 5 — Провода, замок, взлом
`Content.Server/_IS14/Modsuit/`
- [ ] `Wires/ModsuitLockWireAction.cs`, `MalfunctionWireAction`, `ShockWireAction`, `InterfaceWireAction`
- [ ] `ModsuitAccessSystem` — ID-замок, ЭМАГ
- [ ] `ModsuitEmpSystem`
- [ ] Взаимодействия инструментами (отвёртка/лом/ключ) — в shared

### Фаза 6 — Визуал модулей
`Content.Client/_IS14/Modsuit/`
- [ ] Спайк: вставка слоёв в спрайт носителя поверх части
- [ ] `ModuleWornOverlayComponent` + `ModsuitVisualsSystem`
- [ ] Оверлей на предмете-контроллере (иконка костюма)
- [ ] Покраска и смена скина

### Фаза 7 — Контент
`Resources/Prototypes/_IS14/`
- [ ] `Entities/Modsuits/base.yml` — базовые прототипы контроллера и частей
- [ ] `Entities/Modsuits/themes/*.yml` — 11 тем MVP
- [ ] `Entities/Modsuits/Modules/*.yml` — каталог из §7.5
- [ ] `Entities/Modsuits/cores.yml`, `parts.yml`, `platings.yml`
- [ ] `Recipes/Construction/Graphs/modsuit.yml` + рецепты лат
- [ ] `Research/modsuits.yml`
- [ ] Локализация `ru-RU` и `en-US`
- [ ] Гайдбук

### Фаза 8 — Миграция
- [ ] Перевести Goob-костюмы (11 шт.) и EE-костюмы (8 шт.) на новую систему
- [ ] Обновить лутспавны, шкафчики, карго, аплинк, лоадауты
- [ ] Удалить `SealableClothing*` и Goob-правки в `ToggleableClothingComponent`,
      откатив апстримный файл к ванильному виду
- [ ] Changelog + запись о breaking changes (переименованные прототипы)

### Фаза 9 — Мехи (отдельный проект)
- [ ] Подключить `ModularChassisComponent` к `MechComponent`
- [ ] Перевести `MechEquipmentComponent` на `ChassisModuleComponent`
- [ ] Переключить UI меха на `ModularChassisWindow`

---

## 15. Спрайты: DMI → RSI

Все нужные DMI лежат в `BandaStation-master/icons/`.

### Обязательно (у нас этого нет вообще)

| # | Файл | Состояний | Что это |
|---|---|---|---|
| 1 | `icons/obj/clothing/modsuit/mod_modules.dmi` | 125 (32×32) | **Иконки модулей как предметов.** Без этого нет каталога модулей |
| 2 | `icons/mob/clothing/modsuit/mod_modules.dmi` | 57 (32×32, 4 напр.) | **Оверлеи модулей** на надетом костюме |
| 3 | `icons/obj/clothing/modsuit/mod_construction.dmi` | 33 (32×32) | Ядра (стандарт/плазма/эфирное/бесконечное/душа), стадии сборки, обшивки по темам, набор для перекраски, аппликатор скина |

### Желательно (расширяет набор тем)

| # | Файл | Состояний | Что это |
|---|---|---|---|
| 4 | `icons/obj/clothing/modsuit/mod_clothing.dmi` | 323 (32×32) | Предметные спрайты **всех 32 тем**. У нас есть 19 — не хватает: mining, loader, research, rescue, safeguard, magnate, cosmohonk, syndicate, elite, infiltrator, interdyne, ninja, prototype, glitch, chrono, advanced, civilian, psuit, asteroid, corpsman, honkerative, enchanted, debug |
| 5 | `icons/mob/clothing/modsuit/mod_clothing.dmi` | ~300 (32×32, 4 напр.) | Надетые спрайты тех же тем |

### Не нужно

`icons/bandastation/mob/species/{skrell,tajaran,vulpkanin}/clothing/modsuits.dmi` — видовые
варианты под расы, которых у нас нет.

### Важно про лицензию

Спрайты `/tg/station` и BandaStation распространяются под **CC-BY-SA-3.0**, а не под AGPL.
При конвертации в каждый `meta.json` обязательно:

```json
"license": "CC-BY-SA-3.0",
"copyright": "Taken from tgstation at commit <hash>, https://github.com/tgstation/tgstation"
```

Иначе `reuse-updater.yml` и ревью зарежут PR.

### Конвертацию можно не делать вручную

DMI — это обычный PNG со спрайтшитом и метаданными в `zTXt`-чанке `Description`
(размер клетки, список состояний, число направлений, кадры, задержки). Разбор и
нарезка в RSI автоматизируются скриптом — уже проверено на этих файлах.
Ручная работа останется только там, где спрайт нужно доработать под наши пропорции.

---

## 16. Риски и открытые вопросы

| Риск | Оценка | Митигация |
|---|---|---|
| Оверлеи модулей поверх надетой части | **Высокий** — `ClothingVisuals` не сетевой | Спайк в фазе 6 до написания контента; запасной вариант — предзаданные слои-заглушки на частях |
| Объём контента (~40 модулей × локализация × ресёрч) | Средний | Библиотека поведений из фазы 4 обязана быть готова до фазы 7 |
| Конфликт с апстримом при откате `ToggleableClothingComponent` | Средний | Откат делать отдельным PR в самом конце, не смешивая с фичей |
| Производительность: тик расхода на каждом костюме | Низкий | Аккумулировать расход и списывать раз в секунду, а не каждый тик |
| Синхронизация очереди герметизации клиент/сервер | Средний | Предсказуемый `DoAfter` на часть, состояние очереди — в сетевом компоненте |

**Вопросы к обсуждению:**

1. Оставляем ли `Active`-модули с «особым кликом» (кинезис, медлуч) в MVP или откладываем?
2. Нужен ли аналог MODlink (связь между костюмами) — или это отдельная фича?
3. ИИ/пИИ в костюме — сразу или потом?
4. Стартовые костюмы в шкафчиках отделов — или МОД полностью «заработай через РнД»?

---

## 17. Состояние реализации

Сборка зелёная, YAML-линтер чистый, интеграционные тесты
`Content.IntegrationTests/Tests/_IS14/ModsuitTest.cs` (31 шт.) проходят.

| Фаза | Состояние |
|---|---|
| 0 — спрайты и звуки | ✅ 11 тем, иконки и оверлеи модулей, детали сборки, 19 звуков |
| 1 — каркас шасси | ✅ модули, сложность, конфликты по тегам, питание |
| 2 — части, развёртывание, герметизация | ✅ включая частичную герметизацию и оверслот |
| 3 — интерфейс | ✅ дашборд, алерт заряда; радиальное меню — нет |
| 4 — библиотека поведений | ✅ grant / action / item / storage / light / magboots / worn-overlay |
| 5 — провода и ID-замок | ✅ 4 провода, ID-замок, ЭМАГ |
| 6 — визуал модулей | ✅ оверлеи модулей на надетых частях |
| 7 — контент | ◐ 11 тем, 35 модулей, 2 ядра, гайдбук, карго, 3 узла науки |
| 8 — миграция Goob/EE | ⛔ не начата, старые костюмы работают параллельно |
| 9 — мехи | ⛔ отдельный проект |

### Правки после первого игрового теста

| Проблема | Причина | Решение |
|---|---|---|
| Не отображался шлем ни на одном мобе | слот `head` маппится клиентом в `HELMET`, а состояния назывались `equipped-HEAD` | переименованы в `equipped-HELMET`, конвертер исправлен |
| Герметизация прерывалась движением | `BreakOnMove = true` в DoAfter | снято: костюм закрывают на бегу к шлюзу |
| Костюм не надевался поверх одежды | оверслота не было вовсе | у каждой части свой контейнер-заначка; вытесненная вещь возвращается при сворачивании |
| Фонарь светил во все стороны | `PointLight` без маски | конус `cone.png` + `autoRot` объявлены на прототипе шасси |
| Нет звука стравливания воздуха | звук был только на всю последовательность | `seal_part` / `unseal_part` из `compressed_air` на каждую часть |

### Правки после второго игрового теста

| Проблема | Причина | Решение |
|---|---|---|
| Фонарь не включался | `netsync: false` на `PointLight` шасси: свет переключается серверным сообщением UI, а не предсказуемым действием, поэтому клиент о включении не узнавал | netsync снят, состояние света реплицируется |
| Магнитки не давали статус справа | модуль выдавал только `NoSlip` — ни алерта, ни отмены невесомости | поведение `ModuleMagboots` поверх штатного `SharedMagbootsSystem.UpdateMagbootEffects` |
| Железо модуля не отличить от подобранного | устройства спавнились из общих прототипов | свои прототипы `IS14ModsuitHealthAnalyzer` / `IS14ModsuitMiningDrill` со значком МОД-шлема в правом нижнем углу иконки |
| Устройство можно было потерять | `UnremoveableComponent` просто запрещал выбросить, без обратной связи | `ChassisDeviceComponent`: попытка выбросить, кинуть или убрать в сумку отменяется и следующим тиком складывает модуль |
| Интерфейс читался как таблица | вкладки со списками | дашборд: кукла обшивки с раскрывающимся меню части, батарея-ячейка, сегментный бюджет сложности, карточки модулей |

### Правки после третьего игрового теста

| Проблема | Причина | Решение |
|---|---|---|
| Хранилище не открывалось | модуль включался только на запечатанном костюме, а интерфейса хранилища у блока управления не было вовсе | модуль стал `Usable` с флагом `ChassisInactive`, кнопка «Открыть» в отсеке модулей, `StorageUiKey` добавлен блоку управления |
| Звук герметизации проигрывался пачкой | `PlayPvs` в обработчике do-after: клиент переигрывает предсказание каждый тик и каждый раз заводил новый источник | `PlayPredicted` с тем, кто нажал |
| Костюм оставался включённым после сворачивания | `Active` менялся только в конце последовательности герметизации, а сворачивание её не запускает | состояние шасси пересчитывается в `RefreshChassis`: включён ровно тогда, когда запечатана хоть одна часть |
| Извлечение модулей в ПКМ-меню | вербы дублировали то, чему место в панели | вербы убраны, у каждого модуля в интерфейсе своя кнопка «Извлечь», активная только при открытой панели |
| Осмотр выдавал ID-замок, сложность и панель | обработчики `ExaminedEvent` на шасси и замке | оба сняты — это данные интерфейса, а не то, что видно снаружи |
| У железа модулей был лишний слой поверх иконки | анимация удара копирует спрайт оружия целиком, и состояние из чужого RSI превращалось в синий квадрат | иконка устройства — спрайт самого модуля, спрайты «в руке» берутся у исходного предмета, а принадлежность помечается значком поверх слота руки |

Значок на слоте потребовал двух точечных правок upstream (обе помечены `//IS14-change`):
`SlotControl` получил слой `BadgeRect`, а `HandsUIController` — доступ к кнопке руки
по имени. Рисует значок `ChassisDeviceBadgeSystem`.

### Иконки интерфейса

Взяты из `Resources/Textures/Interface/VerbIcons` — это чёрные силуэты, поэтому
`ChassisStyle.Icon` перекрашивает каждую при вставке. Свой добавлен один,
`_IS14/Interface/VerbIcons/no.png`, для запрещённых действий: у `Spare/no.svg` нет
растровой версии, а движок SVG не читает.

| Действие | Иконка |
|---|---|
| Развернуть / сложить часть | `open` / `close` |
| Загерметизировать / разгерметизировать | `lock` / `unlock` |
| Включить-выключить модуль | `Spare/poweronoff` |
| Выбрать / убрать активный модуль | `pickup` / `in` |
| Разовый модуль | `zap` |
| Извлечь модуль | `eject` |
| Действие недоступно | `_IS14/.../no` |

Модуль может переопределить и подпись, и иконку своей кнопки — `actionText` и
`actionIcon` в `ChassisModule`; так модуль хранения пишет «Открыть» вместо «Использовать».

### Целостность обшивки

Каждая часть носит собственный запас прочности: шлем 250, корпус 300, перчатки 200,
ботинки 200. Урон, попавший в тело под частью, снимает её целостность.

| Часть | Прикрывает |
|---|---|
| Шлем | `Head` |
| Корпус | `Chest`, `Groin` |
| Перчатки | `FullArms` (руки и кисти) |
| Ботинки | `FullLegs` (ноги и стопы) |

Подписка идёт на `InventoryRelayedEvent<DamageModifyEvent>`: удар по мобу ретранслируется
на всё надетое, а `TargetPart` в событии говорит, куда именно пришлось. Считается
`OriginalDamage`, а не текущий `Damage` — обшивка изнашивается от того, что в неё
попало, а не от остатка, который её собственная броня пропустила. Иначе чем лучше
костюм, тем дольше он живёт, что задом наперёд.

`ModsuitPartComponent.DamageMultipliers` умножает урон по типам; по умолчанию
`Ion: 2` — ионное оружие направляют ровно в такие штуки.

Порогов два, и они делают разные вещи.

| Порог | Поле | Что происходит |
|---|---|---|
| 66% | `moduleThreshold` | часть перестаёт отдавать свой `SlotFlag` в `ChassisGetAvailableSlotsEvent`, все модули на этом слоте гаснут через штатный `MissingParts`. Герметичность сохраняется |
| 33% | `unsealThreshold` | часть срывает герметизацию сама и отказывается запечатываться, пока её не починят. Остальной костюм закрывается вокруг неё |

Первый порог — это «модули отвалились, но ты ещё жив». Второй — «беги к шлюзу».
Потерять одну часть при этом можно: `TryToggleSeal` пропускает вскрытые части вместо
того, чтобы упираться, так что костюм закрывается всем, что ещё цело.

### Ремонт

Тип поломки не бросается кубиком, а выводится из того, чем били. Каждая часть копит
две суммы: `structuralWear` (тупой урон, прокол, рез) и `electricalWear` (всё
остальное — лазеры, ион, ток). Что перевесило, того и требует часть:

| Поломка | Чем чинится | Что тратится |
|---|---|---|
| `Structural` — погнутая обшивка | сварка (`Welding`) | топливо горелки, `repairFuel` |
| `Electrical` — выгоревшая проводка | моток кабеля (тег `CableCoil`) | 1 кабель за подход |

Работают по самой части, а не по блоку управления: часть разворачивают, а потом
кликают по её слоту экипировки инструментом в руке. Клик по занятому слоту предметом
в руке — это уже штатное `InteractUsing` по надетой вещи (`InventorySystem.OnUseSlot`),
так что погнутую перчатку чинят на перчатке, а не машут горелкой над ранцем.

Следствие: обслуживают свой костюм, разворачивая часть за частью. Костюм, лежащий на
полу, обслужить нельзя — его части сложены внутрь и до них не дотянуться. Чужой костюм
тоже не починить: меню обыска не пробрасывает `InteractUsing` на надетое.

Один подход возвращает `repairFraction` (25%) от максимума и списывает столько же
накопленного износа своего типа, так что костюм, который и били, и жгли, требует
обоих инструментов по очереди. Не тот инструмент — попап говорит, какой нужен.

Мультитул сюда специально не взят: на костюме есть `WiresPanel`, и `Pulsing` по нему
уже открывает меню проводов.

### Управление и хоткеи

| Ввод | Действие |
|---|---|
| `E` | открыть отсеки модуля хранения (`StorageComponent.OpenOnActivate`) |
| `Alt+E`, альт-клик | открыть панель костюма |
| лом по открытой панели | вынуть ядро |
| сварка / кабель по слоту развёрнутой части | ремонт этой части |
| умное экипирование по спине | отсеки хранения (ядро оно больше не трогает) |

`ActivatableUI` на костюме объявлен как `verbOnly: true` + `altVerb: true`:
`verbOnly` снимает панель с обычного `ActivateInWorldEvent`, освобождая `E`,
а `altVerb` (губ) переносит вербу в альтернативный список, куда бьют и `Alt+E`,
и альт-клик. Ядро вынимается **только ломом**. `ModCoreSystem` отменяет `ItemSlotEjectAttemptEvent`
для слота `mod-core`, когда извлечение затеял человек, и это разом закрывает вербу
«извлечь» в ПКМ-меню, кнопку слота и умное экипирование. Лом идёт мимо: он вынимает
ядро прямым `Container.Remove`, не проходя через `CanEject`. Отмена не трогает случаи
с `user == null` — разрушенный костюм по-прежнему отдаёт ядро наружу.

Побочный и нужный эффект: умное экипирование по спине больше не выдёргивает ядро.
`SmartEquipSystem` сначала пробует слоты предметов, но `TryEjectToHands` теперь
возвращает false, и обработчик проваливается дальше — в ветку хранилища, то есть
костюм с модулем хранения ведёт себя как обычный рюкзак.

### Лом достаёт только ядро

Лом на открытой панели поднимает `ChassisPryEvent`; отвечает `ModCoreSystem` на слое
костюма и отдаёт ядро в руки. Модулей лом больше не касается — у каждого есть кнопка
«Извлечь» в интерфейсе, где состояние панели и так на экране. Событие сначала
поднимается с `DryRun`, чтобы инструмент отказал сразу, а не после двух секунд.

### Как устроен интерфейс

Окно рисует себя из собственной палитры (`Content.Client/_IS14/Modular/Controls/`),
а не из станционного стайлшита — так МОД выглядит как приборка на спине и при этом
не требует правок в upstream-стилях.

- `ChassisStyle` — палитра и фабрика панелей/чипов.
- `ChassisButton` — кнопка на `StyleBoxOverride`, с состоянием «залипла».
- `BatteryGauge` — заряд как настоящая ячейка: корпус, клемма, 12 сегментов, мигание на нуле.
- `ComplexityBar` — бюджет сложности насечками, чтобы считать оставшееся место глазами.

#### Где переносится текст, а где нет

Описание модуля раскрывается по нажатию на карточку и должно переноситься по строкам.
Два места, где это ломается, и оба неочевидны:

1. `ScrollContainer` с включённой горизонтальной прокруткой (а она включена по умолчанию)
   меряет детей с бесконечной шириной. Текст, которому не сказали, сколько у него места,
   не переносится никогда. Отсюда `HScrollEnabled="False"` на бэе модулей.
2. Горизонтальный `BoxContainer` меряет детей по очереди, вычитая уже занятое. Описание,
   лежащее в одной строке с кнопками, получит при замере и их ширину тоже — и уедет под
   них при отрисовке, потому что рисуется по замеру, а не по разметке. Поэтому карточка —
   колонка: строка «иконка / название / кнопки», а описание отдельной строкой под ней во
   всю ширину.

Слева бумажная кукла: голова сверху, руки и корпус в ряд, ботинки снизу, справа от
корпуса — сам блок управления. Нажатие на плитку раскрывает под куклой меню этой части
(развернуть/сложить, загерметизировать), нажатие на блок управления — состояние железа.

### Осознанные отклонения от плана

1. **Производство — рецепты экзокостюмного фабрикатора, а не граф сборки.**
   Рецепты модулей и ядра при переходе на граф переносятся без изменений.
2. **Мезонного визора нет** — в билде отсутствует компонент мезонного зрения.
   Вместо него модуль ночного визора на `NightVision` из Goob.
3. **Джетпак и стабилизатор плазмы не сделаны.** `JetpackComponent` требует газового
   баллона, а `IgniteFromGasImmunityComponent` срабатывает только по событию надевания —
   оба несовместимы с выдачей компонента «на лету». Нужен свой C#, вынесено в бэклог.

### Ограничение библиотеки поведений

`ModuleGrantComponents` годится только для компонентов, которые работают сразу после
навешивания: маркеры и статы, читаемые через inventory relay или прямой подпиской
(`NoSlip`, `EyeProtection`, `NightVision`, HUD-иконки, `EmpResistance`,
`RadiationProtection`, `TemperatureProtection`). Компоненты, которым нужен собственный
жизненный цикл предмета — событие надевания, свой контейнер, своя цель клика — требуют
отдельного поведения. Это граница, а не баг: проверять её надо до того, как модуль попал
в прототип.

### Расхождения с диздоком, найденные при реализации

- `ClientClothingSystem` строит надетые состояния как `{префикс}-equipped-{слот}`,
  поэтому запечатанные спрайты названы `sealed-equipped-HELMET`.
- Движок допускает **ровно один обработчик на пару (компонент, событие)** во всей игре.
  Из-за этого смена носителя раздаётся модулям ретрансляцией (`ModuleUserChangedEvent`),
  а визуал контроллера дёргается из уже существующего обработчика состояния шасси.
- `StorageComponent` сеточный (`List<Box2i>`); `MaxItemSize` и `MaskPath` у `PointLight`
  закрыты `[Access]` и задаются только в прототипе.
- `ClothingComponent.ClothingVisuals` не сетевой, поэтому оверлеи модулей рисует
  клиентская система через `GetEquipmentVisualsEvent`, а не сервер.

### Правки после четвёртого игрового теста

| Что | Решение |
|---|---|
| Лом вычищал модули по одному вслепую | лом отдаёт только ядро (`ChassisPryEvent`), модули — кнопкой в интерфейсе |
| Ядро стояло сбоку от куклы | ядро в центре второго ряда, корпус справа |
| Части не изнашивались | целостность на каждой части, урон по телу под ней снимает прочность |
| Непонятно, к чему привязан модуль | под иконкой модуля — миниатюры частей из `requiredSlots` с цветом состояния |
| «Руки», «Ноги», «Голова» в кукле | «Перчатки», «Ботинки», «Шлем» |

Заодно исправлена иконка `_IS14/Interface/VerbIcons/no.png`: она была нарисована чёрной,
а `ModulateSelfOverride` умножает цвет — то есть тонировка оставляла её чёрной. Верб-иконки
апстрима белые, теперь и эта тоже. Рядом добавлена `shield.png` для строки целостности:
растрового щита апстрим не поставляет.

### Правки после пятого игрового теста

| Что | Решение |
|---|---|
| Один порог целостности | два: 66% — гаснут модули, 33% — часть срывает герметизацию и не закрывается |
| Разбитую часть нечем починить | сварка для обшивки, кабель для проводки; что нужно — решает то, чем били |
| Кнопки «сложить всё» / «сшить всё» внизу | перенесены в шапку рядом с именем костюма, с теми же иконками, что у отдельных частей |
| Подписи не влезали в левую колонку | окно 900×700, левая колонка 332 |
| Фонарик щёлкал беззвучно | `activateSound` / `deactivateSound` на `ChassisModule` — доступно любому переключаемому модулю из YAML |
| Оверлей фонарика не переключался на «включён» | переключение приходит серверным сообщением интерфейса, клиент видел только смену состояния компонента; добавлена подписка на `AfterAutoHandleStateEvent` |
| `E` открывало панель, ядро снималось альт-кликом | `E` — отсеки, `Alt+E` — панель, альт-клик по ядру отключён |

### Правки после шестого игрового теста

| Что | Решение |
|---|---|
| Фраза «разбита слишком сильно…» в панели части | две строки — «Герметичность» и «Модули» — с галочкой или крестиком у каждой |
| Ремонт по блоку управления | ремонт по самой части: инструмент в руке + клик по её слоту экипировки |
| Непонятно, чем чинить | строка поломки с иконкой молотка (`Interface/hammer_scaled`) |

Галочка и крестик нарисованы в `_IS14/Interface/VerbIcons/` рядом со щитом: в билде
готовых нет, а `Nano/checkbox_checked` — это чекбокс с рамкой, не сама галка. Все три
белые, чтобы `ModulateSelfOverride` их красил.

### Правки после седьмого игрового теста

| Что | Решение |
|---|---|
| Инструмент по целой части давал попап и звук отказа | целая обшивка — это не провал ремонта, а не ремонт вовсе: клик молча остаётся необработанным |
| «держит» / «отказала» рядом с галочками | подписи убраны, галочка и крестик говорят сами |
| Интерфейс не обновлялся при открытии панели | новое `ChassisPanelChangedEvent` из `SetPanelOpen`, на него подписан UI костюма |
| Интерфейс не обновлялся при смене ядра | `ModCoreSystem` поднимает `ChassisPowerChangedEvent` при вставке и извлечении |
| Ядро снималось ПКМ-вербой и умным экипированием | отмена `ItemSlotEjectAttemptEvent` для `mod-core`; лом вынимает ядро мимо `CanEject` |

### Правки после восьмого игрового теста

| Что | Решение |
|---|---|
| Предмет клался в отсеки и через мгновение возвращался в руку | сетка выдаётся уже после создания `StorageComponent`, а маска занятости строится один раз в `ComponentInit` — она описывала пустую сетку, и любой `CanInsert` падал на `comp-storage-insufficient-capacity`. Теперь `ModuleStorageSystem` перестраивает маску после каждой смены сетки |
| Иконка сумки на слоте не появлялась и не исчезала | `ClientInventorySystem` решает это один раз при надевании; `ChassisStorageBadgeSystem` пере-подаёт `SlotSpriteUpdate` по `ChassisModulesChangedEvent` |
| Удаление костюма с вещами в отсеках сыпало ошибками в лог | `ModuleStorageSystem.Stop` не лезет в контейнер уничтожаемого шасси — содержимое уходит вместе с ним |

Первое потребовало третьей правки апстрима: `SharedStorageSystem.UpdateOccupied`
переведён из `protected` в `public` (`//IS14-change`). Публичного способа сообщить
хранилищу, что его сетку поменяли на ходу, в апстриме нет, а маска нужна и при выдаче
сетки модулем, и при её возврате.

### Правки после девятого игрового теста

| Что | Решение |
|---|---|
| Извлечённый модуль падал на пол | `TryUninstall` отдаёт его в руки оператору шасси (`ChassisGetUserEvent`), а `PickupOrDrop` сам роняет на пол, если рук нет |
| Звук герметизации | оба направления играют `_IS14/Modsuit/part_move.ogg` |
| Панель проводов не открывалась | в `UserInterface` блока управления не было `enum.WiresUiKey.Key` — сами вербы, разводка и все четыре действия существовали, но открывать их было некуда |

### Каталог после расширения

Модулей стало 19, тем — восемь плюс отладочная.

**Новые модули** (всё — чистый YAML поверх существующих поведений):

| Модуль | Тип | Что делает |
|---|---|---|
| Диагностический визор | переключаемый | здоровье синтетиков на забрало |
| Навигация | пассивный | `HandheldGPS` на шасси |
| Цепкие протекторы | пассивный | `NoSlip`, требует ботинок |
| Дефибриллятор | активный | `ModuleItem` с одноручным вечнозаряженным дефибриллятором |
| Конструктор | активный | `ModuleItem` с RCD, матрица восстанавливает заряд сама (`AutoRecharge`) |
| Усиление брони | переключаемый | `Armor` на шасси — броня раздаётся ретрансляцией по надетому, а блок управления как раз надет |

**Новые темы**: медицинская, СБ, шахтёрская, атмосферная, научная, кроваво-красная.
Каждая — пять прототипов и строка в рецептах лате; кроваво-красная не печатается
и наследует `BaseSyndicateContraband`. Броня живёт на нагруднике, встроенные модули —
на блоке управления, как у инженерной.

Блоки тем генерировались скриптом: пять прототипов на тему, отличающихся одним листом
спрайтов, руками пишутся ровно до первой опечатки, которая всплывает уже в игре.

### Ключи проводов живут в Shared

Панель проводов кладёт свои `StatusKey` внутрь `WiresBoundUserInterfaceState`, а тот
уходит в сетевое состояние. Ключ, объявленный в `Content.Server`, компилируется,
проходит линтер и стартует без единой жалобы — а потом валит сервер посреди тика,
как только кто-то открывает панель: PVS не может сериализовать тип, о котором клиент
никогда не слышал (`KeyNotFoundException: Type not found`).

Поэтому `ModsuitWireKey` лежит в `Content.Shared/_IS14/Modsuit/` с
`[Serializable, NetSerializable]`, как и все ключи проводов апстрима. Тест
`WireKeysAreNetSerialisable` спрашивает у `IRobustSerializer`, знает ли он этот тип, —
это ловушка, которую не видят ни компилятор, ни линтер.

### Правки после десятого игрового теста

| Что | Причина | Решение |
|---|---|---|
| Дефибриллятор брался одной рукой | взят прототип `DefibrillatorOneHandedUnpowered` | добавлен `MultiHandedItem`; если вторая рука занята, модуль не разворачивается и говорит почему |
| Нечем открыть UI по кнопке модуля | не было такого поведения | `ModuleInterface` — открывает интерфейс модуля по включению и выключает модуль по закрытию окна, так что `activeDraw` капает ровно пока смотрят |
| ПНВ выдавал вторую кнопку включения | `NightVision` наследует `SwitchableVisionOverlay` со своим `ToggleAction` | `toggleAction: null` + `isActive: true` в выдаваемом наборе: выключателем становится сам модуль |
| Непонятные описания | — | у радиозащиты и усилителя брони в описании теперь конкретные числа |
| Активные модули не тратили энергию за работу | `useCost` списывался только у разовых модулей | `ChassisDevicePowerSystem` метит само устройство: списание за удар, за разряд, за постройку — и отказ, если платить нечем |
| Визуал модуля оставался на теле | ключ слоя был `modsuit-module-0` у всех частей, а карта слоёв на спрайте моба общая — снятие одной части уносило чужой ключ, а свой слой оставляло навсегда | ключ включает слот части |
| ЭМИ-защита не работала | `EmpResistance` висел на шасси, а импульс прилетает в то, что держит заряд — в батарею внутри ядра внутри костюма | `ModuleEmpShield` отвечает на `EmpAttemptEvent` за всё, что вложено в шасси |
| Разряженный костюм герметизировался | `HasWorkingCore` проверял только `Max > 0` | проверяется и текущий заряд |

### Модуль как окно

`ModuleInterface` — второй способ «использовать» модуль после выдачи предмета.
Модуль объявляет свой `UserInterface`, поведение указывает ключ, и дальше:

- включение модуля открывает окно оператору шасси;
- закрытие окна выключает модуль, а с ним и `activeDraw`;
- снятие модуля или потеря нужной части закрывают окно.

Первым его получил навигационный модуль: координаты видно всегда (`HandheldGPS`
пассивно на шасси), а по кнопке разворачивается карта станции. Сканеры масс и монитор
экипажа встанут туда же — им нужен только свой `UserInterface` на прототипе.

### Почему устройство метится, а не модуль

Модуль вида «выдать инструмент» не может списывать заряд при активации: бур достают
один раз, а стоит работа. Поэтому `ChassisDevicePowerSystem` подписан на само
устройство — `AttemptMeleeEvent` для замаха и `AfterInteractEvent` для дефибриллятора и
конструктора, — берёт `useCost` за действие и отменяет действие, если костюму платить
нечем. Подписка на `AfterInteractEvent` идёт `before: [typeof(RCDSystem)]`, чтобы
неоплаченная постройка не начиналась вовсе.

### Правки после одиннадцатого игрового теста

| Что | Решение |
|---|---|
| Описания модулей загромождали список | карточка сворачивает описание; клик по ней разворачивает текст с переносами (`RichTextLabel`) и настройки модуля |
| ЭМИ-защита была абсолютной | `ModuleEmpShield` не отменяет импульс, а множит `EmpPulseEvent` на 0.1 — перед `SharedBatterySystem`, который это число и читает |
| Не было где показать баллон | `PressureGauge` — циферблат со стрелкой и красной дугой у нуля, справа от энергоблока |

### Настройки модуля в интерфейсе

`ModuleGetConfigEvent` существовал с самого начала, но окно его не рисовало. Теперь
`Bool`, `Choice` и `Button` рендерятся внутри раскрытой карточки. Новый настраиваемый
модуль не требует ни строчки кода окна — достаточно ответить на событие.

### Атмосферика

Модуль вешает `GasTank` на шасси. Из этого само собой следует всё остальное: внутреннее
дыхание подключается к костюму как к баллону, а `JetpackSystem` требует `GasTank` на той
же сущности, что и `Jetpack`, — поэтому джетпак-модуль работает только вместе с
атмосферным. Компрессор живёт на сервере (атмосфера есть только там), тикает раз в
секунду, тянет выбранные газы из тайла до `targetPressure` 1013.25 кПа и платит зарядом
за реально перенесённые моли. На тайле всегда остаётся резерв — костюм, способный
откачать комнату, был бы оружием, а не модулем.

Выбор газов — обычные настройки модуля, то есть та же раскрытая карточка.

### Плазменное ядро

`ModCoreFuel` принимает стеки по типу, а не по тегу: `Plasma` за 400 Дж лист,
`PlasmaOre` за 150 Дж кусок. Кормить можно и само ядро в руке, и надетый костюм —
второе важнее, иначе ядро пришлось бы выковыривать ломом каждый раз.

### Два хранилища на одном костюме

У сущности может быть только один `StorageComponent`, поэтому второй модуль хранения
на шасси не помещается в принципе. Отсюда `ModuleStorage.onChassis`: общие отсеки
по-прежнему висят на костюме (их открывает `E`, они переживают замену модуля на
больший), а специализированный мешок несёт сам модуль. Такой мешок уходит вместе с
модулем со всем содержимым — это не потеря, а логика: вынули мешок — вынули с рудой.

Размер предмета у такого модуля объявлен `Large`: хранилище без явного предела берёт
предметы на размер меньше себя, и маленький модуль отказал бы обычному куску руды.

Тег у рудного мешка свой — `IS14ModsuitOreStorage`. С обычными отсеками он уживается,
со следующим рудным мешком (бэ-эс версией) — нет.

### Новые модули этого круга

Атмосферика малая и большая, джетпак, рудный мешок, компактность (даёт костюму слот
пояса, конфликтует с отсеками), тэ-скан, перцовый отпор. Перцовый отпор ставит метку на
носителя и отвечает на `AttackedEvent` и `DisarmedEvent` — оба поднимаются на человеке,
а толчок до ретрансляции по надетому не доходит вовсе.

### Правки после двенадцатого игрового теста

**Баллон живёт по установке, а не по герметичности.** Раньше модуль атмосферики
следовал `Enabled`, то есть запечатанному нагруднику, и расшитый костюм терял
`GasTankComponent` вместе со всем газом — интерфейс честно писал «баллона нет».
Отсюда третий режим жизни поведения, `ModuleBehaviourSystem.FollowsInstallation`:
начинается по `ModuleInstalledEvent`, кончается по `ModuleUninstalledEvent`, и
включение-выключение шасси его не касается. Пока что он нужен ровно одному поведению,
и это правильное соотношение — почти всё остальное обязано гаснуть вместе с костюмом.

Герметичность теперь управляет двумя вещами вместо одной:

- **запечатанный нагрудник** — компрессор (это по-прежнему `Enabled` модуля через
  `requiredSlots: [OUTERCLOTHING]`, никакого нового кода);
- **герметичность целиком** — дыхание. `IsFullySealed` строже, чем `IsSealed`:
  требует, чтобы все четыре части были и развёрнуты, и запечатаны, и не вскрыты.
  Шлем при этом получает `BreathToolComponent`, и дальше работает штатная механика
  внутреннего дыхания. Компонент сам подключается на инициализации (правка Goob в
  `LungSystem`) и сам отключает внутренние при выключении, так что снимать его
  достаточно через `RemComp`.

Газ из баллона при снятии модуля не исчезает: `Stop` кладёт смесь в
`ModuleGasTankComponent.Stored`, `Start` возвращает. Модуль — это и есть бутылка,
переставить её в другой костюм должно быть переносом воздуха, а не сливом на пол.

### Манометр стал приборной панелью

Компрессор и клапан переехали из настроек модуля на сам манометр: давление, на которое
они влияют, там уже нарисовано. Никакого нового канала сообщений при этом не появилось —
кнопки шлют тот же `ChassisConfigureModuleMessage` с ключами `pump` и `valve`, что и
карточка модуля, просто состояние знает `TankModule` в стейте. Выбор газов остался в
настройках модуля: это настройка, а не рычаг.

Рядом с давлением — температура в цельсиях и состав: три крупнейшие доли по имени и
проценту, остальное одной строкой «прочее». Имя и цвет газа клиент берёт из
`GasPrototype`, поэтому список работает для любого газа, а не только для тех трёх,
что умеет фильтровать модуль.

### Сложность переехала к модулям

Полоса бюджета стояла под батареей, хотя тратят его модули. Теперь она в шапке отсека
модулей, пустые ячейки рисуются контуром, а не серой заливкой: пустая розетка читается
как «влезет ещё один», серый прямоугольник — как «занято непонятно чем».

Единицы измерения на карточках стали значками: сложность — иконка связанных узлов
(`_IS14/Interface/VerbIcons/complexity.png`), потребление — молния. Подпись вида
«0.2 Вт покоя» превращала карточку в предложение; отличие покоя от работы теперь несёт
цвет и подсказка. Тип модуля уехал из верхнего правого угла в нижний правый, к цифрам:
имя ищут, тип — подтверждают.

### Рудный мешок стал магнитом

Мешок теперь `Toggleable`: включённый тянет руду с пола на ходу. Своё поведение
(`ModuleMagnet`), а не апстримный `MagnetPickup` — тот заведён на `ItemToggle` и на
отметку времени, поставленную в `MapInit`, чего у выданного посреди раунда модуля нет.
Заодно свой код позволил брать плату за подъём: пустой проход бесплатен, удачный стоит
заряда. Тянет он ровно то, что примет сам мешок — белый список у хранилища один.

Кнопка «открыть» у такого мешка не работала по простой причине: сетка живёт на модуле,
значит и окно должно открываться на модуле, а `UserInterface` с ключом хранилища был
только у костюма. Теперь запись есть у модуля, а сама кнопка переехала в настройки
(`ModuleStorage` отдаёт `open` как `Button`) — у включаемого модуля переключатель занят
магнитом.

### Правки после тринадцатого игрового теста

**Альт-клик снова открывает панель.** Выданный костюму `GasTankComponent` вешает на него
свою альтернативную вербу «открыть клапан», а альт-клик выполняет верб с наибольшим
приоритетом — при равных нулях побеждал алфавит. У `ActivatableUIComponent` приоритета
не было вовсе, поэтому он появился: `verbPriority` (правка в маркерах, две строки в
системе и поле в компоненте), у костюма выставлен в 10. Это лечит класс проблемы, а не
один случай: всё, что ещё повесит альт-вербу на костюм, теперь проигрывает панели.

**Клапан и компрессор перестали спорить.** Открытие клапана глушит компрессор, включение
компрессора закрывает клапан — качать в открытую бутылку означает качать в комнату.
Плюс серверный компрессор сам отказывается работать при открытом клапане: клапан можно
дёрнуть вербой мимо панели, и об этом решении панель бы не узнала.

**Энергоблок стал вертикальным**, показания под ячейкой, а не сбоку — батарея высокая,
и цифры читаются под ней так же, как на самой ячейке. Освободившаяся ширина ушла
манометру.

**Строка герметичности.** Вместо объяснения «компрессору нужен запечатанный нагрудник»
в панели баллона стоит статус «Герметичность среды» с галочкой или крестиком: это
предусловие и для компрессора, и для внутреннего дыхания, поэтому у него своя строка,
а не повторение внутри каждого сообщения, которое от него зависит. Само объяснение
осталось подсказкой.

### Установленный модуль доступен через панель

Окно хранилища рудного мешка роняло сервер ассертом в `SharedInteractionSystem`.
Причина не в хранилище: у интерфейса есть радиус взаимодействия, и он проверяется не
только по расстоянию, но и через `IsAccessible`. Модуль лежит в закрытом контейнере
внутри костюма, поэтому обычные правила доступности говорят «дотянуться нельзя» — и это
верно про руки, но неверно про панель, через которую вообще всё это нажимается.
Интерфейс, открытый на недостижимой сущности, закрывается на следующем кадре, о чём
движок и предупреждает ассертом.

Лечится штатной точкой расширения: `ChassisModuleComponent` отвечает на
`AccessibleOverrideEvent` и одалживает ответ у шасси — кто дотянулся до костюма, тот
дотянулся и до того, что в него вкручено. Тот же ответ используется при периодической
проверке радиуса, так что окно больше и не закрывается само.

### Газ носит с собой тепло, а состояние окна строит только сервер

Две мелочи из одного корня — из того, что смесь газа networked-полем не является.

**Компрессор набивал баллон холодом.** `AdjustMoles` переносит моли, но не теплоту:
свежая `GasMixture` начинается с 2.7 K, и сколько бы воздуха из комнаты в неё ни
переложили, она такой и оставалась. Теперь компрессор собирает добычу в отдельную смесь
с температурой окружения и вливает её через `AtmosphereSystem.Merge`, который смешивает
температуры по теплоёмкостям. Заодно поменялся порядок: сначала плата, потом передача —
если заряда не хватило, воздух возвращается в комнату целиком, а не выкорчёвывается
обратно из уже перемешанной смеси. И предел набора считается не по «пока давление
меньше целевого» на каждом шаге, а один раз, по свободному объёму при температуре
приходящего газа.

**Давление мигало нулём при герметизации.** `UpdateUi` — общий код, и клиент во время
предсказания честно собирал состояние из своих компонентов и пушил его себе же.
`GasTankComponent.Air` не сетевое поле, у клиента оно пустое, отсюда «0 кПа» на кадр
до следующего серверного пуша. Состояние этого окна серверное по своей природе, поэтому
`UpdateUi` теперь на клиенте выходит сразу.

### Дыхание и джетпак: железо без переключателя

Обе поломки одной природы. Компонент, выданный посреди раунда, не проходит через
события надевания и инициализации карты — а именно они у апстрима раздают переключатели.

**Дыхание.** Костюм всё делал правильно: при полной герметичности шлем становится
`BreathTool`, баллон на спине находится штатным `FindBestGasTank`. Не хватало двух вещей.
Первая — `UseDelay` с записью `gasTank`: `ConnectToInternals` дёргает эту задержку и без
неё молча возвращает false, так что не работала и стандартная тревога-переключатель.
Вторая — `outputPressure`: у компонента по умолчанию целая атмосфера, а дыхательные
баллоны отдают 21.3 кПа, иначе костюм уходит в лёгкие в несколько раз быстрее, чем нужно.

Плюс кнопка «Дышать из баллона» рядом с манометром. Тревога в углу HUD работает и так,
но костюм, который становится дыхательным аппаратом только при всех запечатанных частях,
угадыванием не находится.

**Джетпак.** Выдавался `ModuleGrantComponents`, то есть появлялся `JetpackComponent` — и
всё. Включает джетпак `SetEnabled`, который вызывается из хотбар-действия, а действие
раздаётся по `GetItemActionsEvent` при надевании предмета. Компоненту, появившемуся
после надевания, его никто не выдаёт: железо стояло, тяги не было. Теперь у модуля своё
поведение, и его собственный переключатель и есть переключатель джетпака. Расход снижен
с движкового умолчания 0.012 до 0.0015 моль за импульс — бутылка костюма вчетверо меньше
настоящего ранца.

Отказ включиться видит только сервер (только он знает, сколько газа в баллоне), поэтому
пустой баллон отвечает всплывающей подсказкой, а не мёртвым тумблером.

### Джетпак: тумблер взводит, невесомость стреляет

Ручной джетпак зажигают и гасят вручную, и зажжённый на полу он ставит носителю
`BodyStatus.InAir` — отсюда и потеря управления при ходьбе. Модулю так вести себя нельзя:
его тумблер лежит в панели, и открывать панель по дороге из шлюза никто не станет.

Поэтому тумблер модуля теперь только **взводит** двигатели, а зажигает их невесомость.
На носителя вешается маркер `ChassisJetpackUser`, который слушает
`WeightlessnessChangedEvent`: шагнул за борт — включилось, вернулся на палубу —
выключилось, положение тумблера при этом не трогается. Снимать тягу нужно с того, на ком
она была заведена (`JetpackComponent.JetpackUser`), а не с текущего носителя костюма —
иначе передача костюма оставляет летать предыдущего.

### Ядро в панели: батарея и бункер

**Извлечение батареи.** Кнопка под показаниями энергоблока, по тому же правилу, что и
извлечение модуля: нужна открытая панель. Само ядро по-прежнему достаётся только ломом.

**Бункер вместо встроенной сетки.** Полноценная сетка инвентаря внутри окна МОДа
означала бы вытащить `StorageContainer` из клиентского контроллера хранилищ вместе с
его драг-н-дропом и предположением «окно хранилища одно» — несоразмерно много хрупкого
клиентского кода ради одной панели. Вместо этого у плазменного ядра собственное
хранилище 8×2, которое открывается кнопкой из окна МОДа: то же перетаскивание, тот же
штатный контрол, отдельное окно. Ядро сжигает содержимое само, по мере надобности,
раз в секунду и только на сервере — клиент, предсказывающий сжигание чужого топлива,
предсказывал бы его удаление.

Что влезет в бункер, решает та же таблица топлива, по которой ядро и заряжается:
второго списка нет, и научившееся новому топливу ядро примет его в том же движении.

**Предел размера предмета** у выданного на ходу хранилища приходится задавать явно —
без него он выводится из размера самой сущности, а ядро и модуль маленькие. Для этого в
`SharedStorageSystem` добавлен `SetMaxItemSize` (поле закрыто анализатором доступа);
рудный мешок с этого перешёл с раздувания собственного размера предмета на честный
`maxItemSize`.

**Обновление показаний.** Топливо пишется прямо в батарею ядра, мимо энергосистемы
шасси, поэтому панель не узнавала о нём до следующего события. Появился
`ChassisPowerSystem.NotifyChargeChanged` — заправка дёргает его и панель обновляется.

### «Нет ядра» и «нет батареи» — разные вещи

С точки зрения энергосистемы пустое гнездо и ядро с вынутой ячейкой выглядят одинаково:
заряд ноль, максимум ноль. Делать с ними надо разное, поэтому сообщение у них теперь
тоже разное, а панель показывает ровно одну из двух кнопок — ту сторону, в которую слот
сейчас может поехать: «Извлечь батарею» или «Вставить батарею» (из руки). Обе по правилу
открытой панели, как и извлечение модуля. Извлечение кладёт ячейку в руку, а не под ноги.

### Замок и саботаж сообщают о себе

`SharedModsuitLockSystem` не может дёрнуть панель напрямую: система костюма уже зависит
от него, обратная зависимость замкнула бы круг. Поэтому появился
`ModsuitSecurityChangedEvent` — одно событие на весь набор (замок, стёртый доступ,
сломанный интерфейс, сбой, электрификация), а не по одному на каждый переключатель.
Всё, что оно покрывает, видно только через панель, а панель всё равно перерисовывается
целиком.

## 18. Как достать человека из МОДа

До этого раздела костюм был неприступен: развёрнутые части неснимаемы, сам костюм не
снимается, пока хоть одна развёрнута, а надетый на чужой спине костюм физически нельзя
кликнуть — клик попадает в тело. У СБ не было ни одного хода. Здесь их пять, и все они
требуют, чтобы носитель был обездвижен, а не избит.

### Досягаемость

На носителя вешается `ModsuitWearerComponent`. Инструмент или карта, применённые к
человеку, пересылаются костюму — и дальше работают ровно те взаимодействия, которые у
отвёртки, мультитула, лома и карты уже есть с МОДом, лежащим на столе. Плюс костюм
считается достижимым (`AccessibleOverrideEvent`), иначе ни do-after панели, ни окно
проводов не откроются: надетая вещь лежит в чужом инвентаре, куда обычные правила
доступа не пускают.

И то и другое — только пока носитель **обездвижен**: в наручниках или не способен
действовать (оглушён, крит, мёртв). Намеренно не «ранен»: чтобы вытащить человека из
МОДа, его нужно скрутить, а не бить.

Читать панель костюма чужому по-прежнему нельзя (`ActivatableUIOpenAttemptEvent`) —
иначе всё остальное теряет смысл: нажал «сложить всё» и готово.

### Расшитую часть можно снять

`Unremoveable` теперь навешивается герметизацией, а не развёртыванием. Закрытая часть
держится на теле, открытая — снимается в меню обыска.

Снятая часть уходит **в костюм, а не в руки** обыскивающему: костюм ей владеет, а нужен
обыскивающему человек внутри. Иначе `Parts` ссылался бы на плитку, живущую где-то в мире,
и костюм становился бы неремонтопригодным. Для этого в `SharedStrippableSystem` появился
маркированный хук `StrippedItemRemovedEvent` — до снятия, а не после: костюм сам двигает
часть, и наполовину выполненный unequip под ним оставил бы плитку на полу.

Сам носитель стянуть с себя часть руками не может — у него для этого есть кнопка
складывания, а руками он бы рассыпал костюм по полу.

### Пять способов открыть

1. **Обесточить.** Лом в открытую панель достаёт ядро, костюм расшивается сам, части
   снимаются по одной. Ненасильственно, надёжно, ничего не ломает. Костюм при этом не
   складывается сам — снимать всё равно придётся руками.
2. **Провод аварийного сброса.** Импульс — костюм отпускает носителя целиком.
   Перерезанный — сброс не работает больше никогда, и тот, кто ждёт ареста в МОДе,
   перережет его заранее. Раскладка проводов случайная: нужен мультитул и готовность
   ошибиться.
3. **Карта доступа по открытой панели.** Не переключение замка, а override: департамент
   забирает своё железо у того, кто в нём стоит. Костюм СБ открывается картой СБ,
   краденый синдикатский — ничем.
4. **Резак.** Сварка по надетой плитке, урон идёт только плитке. Целится по кукле
   прицеливания: режется та часть, которая закрывает выбранную зону. Ниже 33% часть
   расшивается сама и снимается. Долго, шумно и стоит департаменту нагрудника — зато
   заключённого при этом никто не трогает.
5. **Подождать.** Запечатанный костюм ест заряд. Камера и время работают сами.

### Контригра владельца

Модуль ЕМП-щита, перерезанный заранее провод сброса, стёртый список доступа (тем же
проводом — тогда карта не поможет), заряженное ядро и, главное, не попадаться
обездвиженным. Ни один из пяти способов не работает на человеке, стоящем на ногах.


---

## 19. Круг четырнадцатый: костюм в раунде

Первый круг не по итогам игрового теста, а по итогам разбора: сверили сделанное с планом
и с тем, как модульная система устроена у /tg/ (через `BandaStation`). Нашлось не столько
недостающих фич, сколько недостающих **путей к ним**.

### Главное: МОДа не было в раунде

`IS14Modsuit*` вне собственных файлов встречался ровно дважды — в `lathe.yml` и в
`wires_modsuit.yml`. Ни шкафчиков, ни вендоров, ни карго, ни лоадаутов, ни спавнов на
картах, ни ERT. Тринадцать кругов работы лежали в файлах, а не в игре.

Закрыто ящиками карго — `Resources/Prototypes/_IS14/Catalog/`. Выбор именно карго не
случаен: это единственный способ вывести костюм в раунд, не тронув ни одного апстримного
прототипа. Шкафчики и лоадауты потребовали бы правок в чужих `Fills`, и это следующий
шаг, а не этот.

Восемь ящиков — по одному на профильную тему плюс ядра и модули. Цены выше отдельного
скафандра: МОД совмещает скафандр, броню и инструменты, и заказ должен быть решением.

### Исследования

Все 35 рецептов лежали в `IS14ModsuitStatic` и печатались с первой минуты — синдикатский
модуль стоил ровно столько же усилий, сколько фонарик.

Разложено на четыре уровня. Статикой остался входной билет: стандартное ядро, простой
костюм, фонарь и хранилище — без этого систему в раунде просто не увидят. Остальное — три
узла в Industrial (`IS14ModsuitTechnology` → `Specialization` → `Advanced`), позиции в
свободных клетках слева от ветки утилизаторов.

Динамический пак `IS14ModsuitDynamic` пришлось прописать станку отдельно: рецепт,
открываемый технологией, должен быть станку известен заранее.

### Алерт заряда

Значился галочкой в Фазе 3 и не был поставлен. Теперь заряд ядра висит в строке
состояний носителя, пока костюм надет: `IS14ModsuitCharge` на 11 ступеней и
`IS14ModsuitChargeNone` для «ядра нет или в нём пусто».

`clientHandled: false`, в отличие от борга: у борга клиент читает ячейку сам, а у нас
расход считает только сервер (`ChassisPowerSystem.Update`), и алерт приезжает состоянием.
Обновляется из `ChassisPowerChangedEvent` — то есть каждую секунду расхода, при смене
ядра и при смене носителя; `ShowAlert` идемпотентен, повтор того же состояния он
отбрасывает.

Побочно нашлась дыра: слот ячейки живёт на **ядре**, а не на шасси, поэтому замена
батареи не доходила ни до интерфейса, ни до алерта. Добавлена подписка
`ModCoreComponent + PowerCellChangedEvent`.

### Герметичный шлем и еда

Закрытый шлем не мешал есть и пить — в `sealedComponents` не было `IngestionBlocker`.

Исправлено не через `sealedComponents`, а отдельным `RefreshFace` рядом с
`RefreshBreathing`: у компонента должен быть один владелец, а модуль пищевого аппарата
может появиться и исчезнуть, не расшивая шлем. `RefreshFace` зовётся и из
`RefreshChassis` (изменилась герметизация), и из `ChassisModulesChangedEvent` (изменился
состав модулей) — второе выяснилось тестом, который на первой версии падал.

### Новые модули

| Модуль | Как сделан |
|---|---|
| Пищевой аппарат | маркер `ModuleFaceOpening`, читает суитовый слой |
| Монитор экипажа | `ModuleItem` + устройство, как аптечный анализатор |
| Кобура | `ModuleStorage` 2×2 с вайтлистом по `Gun` |
| Храповой привод | своя система: заряд с пройденного расстояния |
| ДНК-замок | своя логика в `SharedModsuitLockSystem` |

Кобура и ДНК-замок оба `allow: ChassisInactive`: оружие должно доставаться, а замок —
держать и на обесточенном костюме.

### Храповой привод

Заряд с каждого метра, `0.1` за метр — при обычной ходьбе это примерно базовый расход
костюма и заметно меньше расхода со включёнными модулями. Отсекается два случая: прыжок
больше двух метров (шаттл, телепорт, бросок — не шаг) и невесомость (храповику не от чего
отталкиваться, и заодно привод не оплачивает джетпак).

### ДНК-замок

Замок на человека, а не на карту. Отпечатанный костюм **не разворачивается** ни для кого,
кроме хозяина — проверка стоит в `TryDeployPart`, потому что это единственная точка, через
которую проходят все пути к развёрнутой части.

Намеренно не блокирует складывание: замок, способный запереть человека внутри костюма,
сломал бы всю главу про то, как носителя достают.

Ломается двумя способами и оба важны: ЭМИ стирает отпечаток (граната по человеку в МОДе —
шестой способ вскрытия), ЭМАГ сжигает модуль насовсем. ЭМАГ раздаётся с костюма, а не с
модуля: модуль лежит в контейнере, ткнуть в него эмагом невозможно, а панель — и есть
законный путь внутрь.

### Что нашлось при прогоне тестов

Тесты не запускались тринадцать кругов. Из 29 упал один — и не от новых правок.

`EmpShieldCoversTheCell` проверял, что щит **отменяет** импульс. Щит давно его не
отменяет, а глушит вдесятеро — тест устарел вместе со сменой реализации. Но, поправив
утверждение, обнаружили настоящую ошибку: щит вообще не включался на неактивном костюме,
потому что у модуля не было `allow: ChassisInactive`. Экран вокруг силовой цепи — это
клетка, а не прибор: ЭМИ-граната прилетает как раз в тот костюм, который выключен.
Исправлено в прототипе; заряд при этом по-прежнему тратится только на работающем шасси,
потому что обесточенное вообще ничего не расходует.

Итог круга: 31 тест, линтер чистый, сборка зелёная.

### Что осталось из разбора

- **Шкафчики, вендоры, лоадауты** — потребуют правок в апстримных `Fills`.
- **Покраска и скины** (Фаза 6) — у /tg/ это набор для перекраски и одноразовый
  аппликатор скина.
- **Радиальное меню** выбора активного модуля (Фаза 3).
- **`ModuleSpeedModifier`, `ModuleTargetedAction`, `ModuleAlert`** — поведения из Фазы 4.
- **Стабилизатор плазмы**, мезонный визор (нет компонента в билде).
- **ИИ в костюме** и **MODlink** — отдельные проекты, самые заметные фичи /tg/, которых
  у нас нет.
- **Фаза 8**, миграция Goob/EE.

## 20. Круг пятнадцатый: взлом переезжает в окно обыска

Три вещи, которые давно не сходились: работать по костюму на чужой спине приходилось
кликом по человеку (то есть вслепую, «в какую-то из частей»), любой такой клик упирался в
жёсткие ворота «сначала обездвижьте», а резак требовал по четыре отдельных нажатия на одну
часть. Все три чинятся одним переездом.

### Предмет по предмету в меню обыска

В костюме на спине каждая железка уже имеет свой слот в окне обыска: сам МОД в слоте
спины, обшивка — в слотах головы, торса, рук и ног. Это готовый прицел, и не хватало
только права по нему кликнуть инструментом.

Апстримный `SharedStrippableSystem` умел ровно две вещи с нажатым слотом: вынуть или
вложить. Добавлена третья — перед этим выбором на предмет в слоте поднимается
`StrippedItemInteractUsingEvent` (`Content.Shared/_IS14/Strip/`), если в активной руке
что-то есть. По умолчанию на него никто не подписан, так что окно обыска для всей
остальной игры не изменилось ни на кадр; подписываются только `ModsuitControlComponent`
(отвёртка, кусачки, мультитул, лом, карта) и `ModsuitPartComponent` (резак).

Правка в апстриме — восемь строк под маркерами. Это тот случай, когда обобщённая точка
расширения честнее, чем частный костыль: «использовать предмет на предмете в инвентаре»
понадобится не только МОДу.

### Обездвиженность стала ценой, а не дверью

`IsSubdued` больше не запрещает работу — он её удешевляет. Носитель на ногах вырывается, и
любое действие по нему идёт втрое дольше (`StandingPenalty`). Наручники, оглушение и крит
убирают штраф целиком, так что подготовка окупается ровно так же, как раньше, но неудачная
попытка теперь стоит времени, а не даёт всплывашку «нельзя».

Из-за этого пришлось завести собственный do-after на панель: у `SharedWiresSystem` задержка
берётся прямо из прототипа, и умножить её снаружи негде. Переключение панели по-прежнему
делает система проводов (`TogglePanel`), нам принадлежит только время.

Одни ворота остались: снять расшитую часть с носителя, стоящего на ногах, нельзя
(`OnPartUnequipAttempt`). Это уже не работа инструментом, а раздевание, и штраф ко времени
там ничего бы не значил.

### Резак режет до дырки

Один проход резака больше не требует нового клика. `ModsuitCutDoAfterEvent` повторяется
сам, как поедание еды, пока не выполнится условие остановки — `IsPartRuptured`, то есть та
самая разгерметизация, ради которой резак и берут. Дальше грызть обшивку смысла нет.

Всплыла настоящая ошибка в апстриме: `SharedToolSystem` заворачивает наше событие в
`ToolDoAfterEvent`, а do-after смотрит на `Repeat` только у обёртки — флаг внутреннего
события молча терялся. Одна строка под маркерами это чинит для всех инструментов сразу.

Обратная сторона `Repeat`: do-after при повторе не перепроверяет инструмент. Погасший или
пустой резак резал бы вечно, поэтому перед каждым следующим проходом мы сами поднимаем
`ToolUseAttemptEvent` — тот же вопрос, который задаёт `CanStartToolUse` на старте.

### Что осталось от старого пути

Клик резаком по самому человеку никуда не делся: там работает кукла прицеливания, и это
по-прежнему самый быстрый способ, когда некогда открывать окно. Отвёртка, кусачки и
мультитул по человеку убраны совсем — им теперь есть куда целиться точнее.

## 21. Круг шестнадцатый: панель кусается

Разбор состояний проводов вскрыл, что три из пяти ярлыков были декорацией: `ТОК` никого не
бил, `ИНТФ` ничего не ломал, у `СБОЙ` работала половина. Этот круг закрывает первый из них
и заодно чинит четыре смежные вещи.

### Резак: вчетверо быстрее проход, вдвое меньше за проход

`CutDelay` 6 → 1.5 с, `CutFraction` 0.25 → 0.125. Проходов стало вдвое больше, каждый
вчетверо короче — суммарно рез быстрее примерно вдвое, но прерывается вчетверо мельче.
Это важно теперь, когда рез повторяется сам: цена шага в сторону — один проход, а не
четверть обшивки.

Повторение при этом не работало вовсе. `RaiseDoAfterEvents` сбрасывает `Handled` у события,
которое видит do-after, — то есть у обёртки `ToolDoAfterEvent`. Внутреннее событие приходило
на второй проход уже помеченным, и обработчик выходил по первой же строке. Проверка на
`Handled` убрана; на `Cancelled` осталась.

### Ремонт больше не сваркой

Сварка одновременно чинила обшивку и резала её же — один инструмент, два противоположных
исхода, и разделяло их только то, по какому слоту попал курсор. Ошибка стоила нагрудника.

Теперь ремонт стоит склада, а не инструмента: **пласталь** вместо сварки за структурные
повреждения, кабель за электрические — как и раньше. Резак остаётся исключительно
инструментом вскрытия, и ни один клик им больше не может означать «починить».

### Броня панели

`WiresPanelSecurityComponent` + граф конструкции, ровно как у шлюза. Три тира —
прутья (4), сталь (2), пласталь (2) — каждый в двух состояниях: положенный и приваренный.
Снимается ломом, приваренный сначала сваркой (5/10/15 с).

Ни один узел графа не задаёт `entity`, поэтому один граф обслуживает все МОДы разом и
подкладка пластины ничего не подменяет. Пришлось уступить лом: `SharedModularChassisSystem`
перехватывал его безусловно, а на забронированной панели лом должен означать «сдирать
пластину». Теперь при `WiresAccessible == false` шасси оставляет клик графу.

Забронировать надетый костюм нельзя — материалы через окно обыска не форвардятся. Это
осознанно: рюкзак на собственной спине не обшивают.

### Провод тока: от чипа в UI до настоящей ловушки

Две пустышки в раскладку — перебор пяти проводов стоил слишком мало.

`ТОК` теперь взводит защиту на 30 секунд и импульсом, и перерезанием: безопасно
попробовать нельзя, что и есть смысл провода, который кусается. Постоянная электризация
как API осталась, но ни один провод её больше не ставит.

Сама ловушка живёт в `Content.Server/_IS14/Modsuit/ModsuitShockSystem.cs` и висит на
`BoundUIOpenedEvent` с ключом `WiresUiKey`. Именно там, а не на `ActivatableUIOpenAttempt`:
интерфейс проводов открывается изнутри `WiresSystem.OnInteractUsing`, мимо всей
ActivatableUI-машинерии, и перехватить его attempt-событием невозможно.

Выбор двери тоже не случаен. Панель самого костюма уже отказывает всем, кроме носителя, так
что ловушка на ней могла бы укусить только собственного хозяина.

Разряд: 25 урона током, 4 секунды паралича, 150 Дж из ядра. Всё через
`ElectrocutionSystem.TryDoElectrocution`, поэтому изолированные перчатки работают сами
собой — контрмера ровно та же, что против решётки под напряжением, и учить игрока новому
правилу не приходится. Пустое ядро не кусается: разрядить костюм — по-прежнему способ его
вскрыть.

Видно это на спрайте: слой `Effects/electricity.rsi` поверх корпуса, тот же, что у решётки.
Показывается он и в окне обыска — то есть ровно там, куда смотрит тот, кто собирается
лезть внутрь.Держится слой на `ElectrifiedVisuals.ShowSparks` через `GenericVisualizer`;
истечение таймера снимает `Update` в `SharedModsuitLockSystem`, потому что срок кончается
по часам, а не по событию, и заметить это некому.

### Что осталось незакрытым из разбора

- **ИНТФ** по-прежнему ничего не ломает: флаг доезжает до UI и на этом всё.
- **СБОЙ** — расход ×3 работает, «модули иногда выпадают» нет.
- Импульс по `ИНТФ` переключает состояние навсегда: у `BaseToggleWireAction` не задан
  `TimeoutKey`, так что мультитулом можно и сломать интерфейс, и починить.

## 22. Круг семнадцатый: костюм заговорил

Четыре доводки поверх предыдущего круга. Три из них — про то, что механика была на месте,
а обратной связи не было.

### Провод тока бьёт того, кто его режет

Перерезание раньше просто взводило защиту, и режущий уходил целым. Теперь `Cut` взводит и
тут же разряжает шасси в того, кто резал. Импульс — только взводит.

Это единственная разница между двумя действиями на этом проводе, и она правильная: провод
не выключатель, а спусковой крючок. Обезвредить МОД, перерезав самый опасный на вид провод,
нельзя — попытка и есть срабатывание.

### Всплывашку заменила речь

`- type: Speech` с роботическим глаголом на контроллере, и три реплики через
`ChatSystem.TrySendInGameICMessage` с `hideChat: true` — машинный шум со словами, а не
разговор, который надо проматывать в логе:

- «Активация защиты контроллера» + короткий бип;
- «Отказ защиты: недостаточно энергии» + звук отклонения;
- «Несанкционированный доступ. Разряд» + молния.

Ловушка стала громкой намеренно. Тихая ловушка наказывает не того, кого надо: взломщик не
понимает, за что его положили, а комната не узнаёт, что рядом взведённый костюм.

### Энергия переехала со срабатывания на взвод

Было: 150 Дж за каждый разряд. Стало: **150 Дж за взвод плюс 5 Вт всё время, пока защита
держится**. Разряд бесплатен — конденсатор уже заряжен.

Это потребовало новой вещи в слое шасси. `ChassisPowerSystem.Update` пропускал неактивное
шасси целиком, то есть взвести защиту и сложить костюм в шкафчик было бы бесплатно. Добавлен
`ChassisGetStandingDrawEvent` — расход, который не зависит от того, работает ли шасси, и
цикл теперь тикает по неактивному шасси, если такой расход есть. Обычный расход модулей туда
класть нельзя: то, что сложенный костюм не ест ничего, — правило, а не случайность.

### Провод сброса стал переключателем

Импульс раньше только отпускал носителя. Теперь он гонит костюм в обе стороны: раскрытый
расшивается и отпускает, сложенный — разворачивается и запечатывается вокруг того, кто его
носит.

Провод перестал быть только аварийным выходом. Тем же импульсом можно закрыть человека в
МОДе, из которого он собирался выйти, — и это, кажется, интереснее, чем односторонняя
кнопка побега.

### Обесточенный костюм расшивается по частям

`OnPowerDepleted` звал `Deactivate`, а тот снимал все печати в одном кадре: беззвучно, без
единого признака, что что-то произошло. Вытащенное ядро приходит сюда же — для цикла расхода
пустая колыбель и есть разряженная батарея.

Теперь это `StartBlowout`: очередь частей и таймер на компоненте, `0.6 с` на часть, каждая
со своим звуком разгерметизации. Не do-after — никто это действие не выполняет, прерывать
нечего и стоять на месте не нужно. `Deactivate` остался мгновенным для тех путей, где костюм
именно вскрывают (провод сброса, карта по открытой панели, снятие).

### Про лампочки

Проверено ещё раз по `GetLightState` всех пяти: перевёрнут только `СБОЙ` — мигает в норме,
гаснет при сбое. У `ЗМК`, `ТОК`, `ИНТФ` и `СБР` свет прямой. Это ровно то, что было в
разборе; менять ничего не потребовалось.

### Провод сброса ронял сервер

`InvalidOperationException` в `SharedDoAfterSystem.Update` — и падал не словарь do-after'ов
(его цикл копирует значения), а перечислитель сущностей на строке 32.

`StartNextSealStep` брал `user ?? Wearer`. Пока герметизацию запускал сам носитель, это было
одно и то же лицо. Провод сброса запускает её от чужого имени: первый шаг вставал на
взломщика, а `OnSealDoAfter` звал следующий уже с `ent.Comp.Wearer` — то есть переносил
do-after на другую сущность. Второй шаг стартует **изнутри** цикла обновления do-after'ов, а
`TryStartDoAfter` там делает `EnsureComp<ActiveDoAfterComponent>` — добавление компонента
инвалидирует запрос, по которому цикл идёт, и сервер умирает в этом же тике.

Порядок перевёрнут: `Wearer ?? user`. Вся последовательность принадлежит телу внутри
костюма, кто бы её ни запустил, — что заодно и правильнее по смыслу: прерывать
запечатывание должен урон по носителю, а не по взломщику, который уже ушёл.

Закрыто тестом `ForceSealRunsOnOneBody`: он проверяет, что после импульса
`ActiveDoAfterComponent` висит на носителе и не висит на взломщике, и прокручивает тики до
конца последовательности — падение было ровно на втором шаге.

### Броня панели роняла сервер на запросе верб

`KeyNotFoundException: 'start'` из `ConstructionGraphPrototype.Path`, по стеку — из
`AddDeconstructVerb`. Всё, у чего есть `ConstructionComponent`, спрашивается о вербе
«разобрать», а та строит путь до `deconstructionTarget`, у которого значение по умолчанию —
литерал `"start"`. Узла с таким именем у графа брони нет и быть не должно: его стартовый
узел называется `mod`.

Поставлено `deconstructionTarget: null` — апстрим этот случай обрабатывает явной проверкой,
и половина прототипов в игре им уже пользуется. Верба «разобрать» на МОДе и не нужна:
пластина снимается ломом, а костюм целиком не разбирается вообще.

Закрыто тестом `VerbsOnAPlatedSuitDoNotThrow`.

## 23. Круг восемнадцатый: панель проводов переписана целиком

Прошлая раскладка складывалась по одному проводу за круг и к концу перестала быть системой:
провод сброса делал слишком много, у замка и сбоя не было пары, а питания не было вовсе.
Набор пересобран с нуля — восемь проводов, из них две пустышки.

### Что теперь в панели

| Ярлык | Цвет | Перерезать | Прозвонить |
|---|---|---|---|
| **ПИТ** ×2 | красный | оба — ядро вон из цепи | перегрузка на 10 с, 10 Вт |
| **РАЗВ** | зелёный | команды развернуть/сложить не доходят | двигает все части |
| **ГЕРМ** | синий | команды герметизации не доходят | запускает процесс в свободную сторону |
| **ТОК** | оранжевый | взводит защиту **и бьёт режущего** | взводит защиту |
| **ИНТФ** | жёлтый | ломает интерфейс | переключает его |

### Два силовых, а не один

Один провод питания был бы просто выключателем. Два дают то, чего у панели раньше не было:
цену за половину работы. Костюм тянет с любого одного, так что первый перерезанный не даёт
ничего, кроме мигающей лампочки, — а лампочка одна на оба и честно показывает, сколько
осталось.

Обесточивание реализовано не флагом «выключен», а разрывом цепи: `ModCoreSystem` перестаёт
отвечать на все три вопроса о заряде. Ядро физически на месте, но снаружи костюм выглядит
ровно как разряженный — и дальше срабатывает всё, что уже написано для разряженного:
`ChassisPowerDepletedEvent`, поэтапная расшивка, снятие защиты, алерт заряда. Ни одной новой
ветки.

Перегрузка от импульса — отдельное состояние с собственным расходом, поверх
`ChassisGetStandingDrawEvent` из прошлого круга. Она ничего не ломает, только жжёт заряд и
пишет о себе в панели: честная награда за угаданный провод.

### Сброс распался на привод и герметизацию

Один провод, который расшивал и запечатывал разом, был слишком силён и слишком тупым. Теперь
их два, и перерезание каждого блокирует **свои** команды:

- **РАЗВ** — обшивка застревает в текущей форме. Рюкзак, который не открыть, или открытый
  костюм, который не сложить.
- **ГЕРМ** — застревает давление. Запечатанный не открыть, открытый не закрыть.

Блокировка стоит на командах, а не на механизме: `CanCommandDeploy` / `CanCommandSeal` в
точках входа игрока — действие на панели действий и сообщения BUI. Внутренние пути
(`RetractAll` при потере питания, расшивка карты по открытой панели) не трогаются: обрыв
сигнальной линии не должен запирать человека в костюме навсегда, у него остаются ещё пять
путей наружу.

### Энергия защиты

Взвод теперь бесплатен, но держится дорого: **10 Вт** всё время и **100 Дж** за каждый
разряд. Костюм, об который вытерли несколько взломщиков, обесточивает себя сам — и это
единственный способ пробиться сквозь защиту, до которой не дотянуться.

### Что снято с вооружения

Пропали два провода, и это заметная потеря — стоит сказать вслух:

- **ЗМК**. Стереть список доступа теперь может только ЭМАГ. Сам ID-замок работает как
  работал: карта по закрытой панели, карта по открытой — приказ отпустить.
- **СБОЙ**. `Malfunctioning` со своим ×3 расходом остался в коде и в API, но ставить его
  больше некому. Ближайший кандидат на замену — ЭМИ.

### Панель «Железо» и блок «Неисправности»

Первая попытка добавила три строки рядом с ядром и панелью: связь по приводу, связь по
герметизации, состояние цепи. Каждая из них почти всё время писала «в норме» — то есть три
строки читаемого места уходили в никуда, и когда что-то ломалось, единственная важная
строка тонула среди докладов о том, что всё хорошо.

Они убраны, а вместо них внизу блока появился отдельный список **«Неисправности»**, пустой
на здоровом костюме. В него попадает всё разом: обрыв питания, перегрузка, сбой контроллера,
обрыв каждой из двух линий обшивки, повреждённый интерфейс и корпус под напряжением —
в порядке от того, чем заниматься первым. Правило простое: если в списке что-то есть, это
проблема, и читать мимо неё нечего.

Плюс два чипа наверху и плитка железа, которая желтеет на перегрузке и обрыве связи и
краснеет на обесточивании, а не только на открытой панели.

### Доводка панели: четыре починки

**Лампочка перегрузки.** Все огни в панели описывают провода, а перегрузка не принадлежит
ни одному: она состояние машины. Апстримный `WiresSystem` такого не умел — словарь статусов
заполнялся только из действий проводов. Добавлен `SetStatus` под маркерами: свет с позицией
`-1`, не совпадающей ни с одним проводом, поэтому в режиме «видно провода» он не получает
чужую букву. Ставится на `MapInit` и обновляется по `ModsuitSecurityChangedEvent`, гаснет, а
не исчезает: пропадающая лампа сообщила бы взломщику, сколько огней бывает.

**Импульс привода на запечатанном костюме.** `ToggleDeployAll` честно расшивал каждую часть
по дороге, потому что так работает кнопка носителя. Для провода это неверно: один угаданный
провод раздевал человека сквозь закрытые печати, и второй провод становился не нужен.
Появился `TryPulseDeploy` — тот же переключатель, но на запечатанном костюме он молча ничего
не делает. Кнопка носителя не тронута.

**Сломанный интерфейс наконец что-то делает.** Флаг доезжал до UI и там и оставался. Первая
попытка починки помогла ровно наполовину, и это стоит запомнить: `ActivatableUIOpenAttempt`
поднимает **только** `ActivatableUISystem`. Панель костюма открывается ещё и с кнопки
действия, а та зовёт `_ui.TryToggleUi` напрямую — мимо всей ActivatableUI-машинерии и,
значит, мимо любой проверки, которая на этом событии висит. Отказ срабатывал по альт-клику и
молча пропускал горячую клавишу, то есть основной путь.

Проверка вынесена в `CanUseInterface` и стоит теперь в трёх местах: на attempt-событии, на
кнопке действия и в каждом сообщении BUI. Последнее не паранойя: окно, уже открытое в момент
обрыва, остаётся окном, и клиент будет из него слать. Плюс `OnSecurityChanged` захлопывает
его на месте — саботаж, которого никто не заметил, саботажем не является.

**Разряд закрывает панель проводов.** Закрытие висело на `BoundUIOpenedEvent`, то есть
срабатывало только на открытии. Перерезавший защитный провод при уже открытой панели получал
удар и продолжал резать с того же места. Закрытие переехало внутрь `Zap`, к самому разряду:
теперь его получает и тот, кто открыл панель, и тот, кто резал, и обоим её захлопывает.

### Пять правок по отчёту с игры

**Компрессор в пятьдесят раз медленнее.** `filterRate` 1.5 → 0.03 и 3 → 0.06. Он должен
подпитывать баллон в течение смены, а не наполнять его, пока стоишь в шлюзе.

**Кобура стала слотом, а не хранилищем.** Была сеткой 2×2 с интерфейсом; теперь это один
слот на нагруднике с тем же вайтлистом, что у наплечной кобуры (`Sidearm`). Понадобилось
новое поведение `ModuleSuitSlot` — оно вешает `ItemSlot` на названную часть обшивки через
`ItemSlotsSystem.AddItemSlot`. Слот следует установке модуля, а не питанию: кобура, которая
опустошается вместе с батареей, — не кобура. Ради него в `SharedModsuitSystem` появился
публичный `TryGetPart`: анализатор доступа не пускает чужие системы в словарь `Parts`, и
выдавать права на запись ради чтения одного элемента было бы неправильно.

**Модуль компактности заработал.** Он менял то, куда костюм можно надеть, но включался
только на работающем шасси — а чтобы шасси заработало, костюм надо надеть. Замкнутый круг:
игроку предлагалось повесить МОД на пояс раньше, чем он поставит модуль, который это
позволяет. Теперь поведение следует установке (`FollowsInstallation`).

**Перцовый отпор ослаблен и покрашен.** `spreadAmount` 10 → 3. Белое облако читалось как пар
из трубы, поэтому у модуля теперь своё, красное.

**ДНК-замок был исправен всё это время.** Кнопка отпечатывает, стирает и снова отпечатывает —
теперь это проверено тестом через `TrySelect`, то есть ровно тот путь, по которому идёт
нажатие в панели, и на сложенном костюме, и на работающем. Сломано было другое: **состояние
замка нигде не показывалось**. Нажатие меняло невидимый флаг, и модуль выглядел мёртвым.
В «Железо» добавлена строка «ДНК-замок: свободен / отпечатан / сожжён», а сожжённый попадает
и в «Неисправности».

## 24. Круг девятнадцатый: т-сканер и новый контент

### Т-сканер не сканировал по двум причинам сразу

Модуль выдавал `TrayScanner` носителю через `ModuleGrantComponents`. Не работало ни одной
половиной.

Первая: клиентский `TrayScannerSystem.Update` ищет сканер, перебирая **слоты инвентаря
игрока и его руки**. Человек не лежит в собственном инвентаре, так что сканера там не
находилось никогда. Сканер должен висеть на костюме — костюм-то как раз надет.

Вторая: подпольные сущности отрисовываются только глазу с маской `VisibilityFlags.Subfloor`,
а её выдаёт `TrayScannerUserComponent`, который апстрим вешает из обработчиков **событий
надевания** ручного сканера. Модуль, поставленный в уже надетый костюм, этих событий не
поднимает, и маски не появлялось.

Обе половины теперь в `ModuleTrayScannerSystem`: сканер вешается на шасси, маска — на
носителя, счётчик `TrayScannerUserComponent` общий с апстримом (чтобы ручной сканер в кармане
не отбирал маску при выключении модуля), и `UserChanged` переносит её при смене хозяина.
Закрыто тестом `TrayScannerModuleScansThroughTheSuit`.

### Три костюма

Арт на них лежал в репозитории неиспользованным с самого начала: `civilian.rsi`,
`loader.rsi`, `magnate.rsi` — полный набор состояний, включая запечатанные.

- **Гражданский** (сложность 10) — самый дешёвый корпус, печатается без исследований.
- **Погрузочный** (17) — карго и утилизаторы, рудный мешок встроен, лёгкая защита от ушибов.
- **Магнатский** (20) — командный, диагностический визор встроен, самый вместительный каркас
  в игре и соответствующий ценник в золоте.

### Шесть модулей

Все на уже существующих поведениях, ни строчки нового кода:

- **Тепловой визор** — четвёртый в семействе визоров, `ThermalVision` по образцу ПНВ.
- **Крепление головных уборов** — первый потребитель `ModuleSuitSlot` помимо кобуры: слот
  на шлеме под шапку.
- **Органайзер** — `ModuleStorage` 4×2 с вайтлистом по бумагам.
- **Уборочная форсунка** и **инъектор** — `ModuleItem` с развёртываемым прибором, как у
  РСД и монитора экипажа.
- **Громкоговоритель** — гообовский `Loudspeaker` на носителе.

Раскладка по науке: бытовые в первый узел, инструментальные во второй, магнатский костюм в
третий. Шапка и органайзер печатаются без исследований — им там нечего защищать.
